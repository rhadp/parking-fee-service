package com.rhadp.parking

import android.app.Application

/**
 * Application class for the PARKING_APP.
 *
 * Currently a minimal shell. Service clients and dependency wiring
 * are handled in MainActivity until a DI framework (e.g. Hilt) is adopted.
 */
class ParkingApp : Application()
