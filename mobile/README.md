# mobile/

This directory is reserved for the **COMPANION_APP** — the Flutter-based mobile application
for remote vehicle control.

The COMPANION_APP allows users to lock/unlock vehicle doors and monitor parking sessions
remotely via the cloud-gateway. It communicates with the backend through the Cloud Gateway
REST/WebSocket API.
