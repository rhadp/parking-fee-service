# Agent-Fox Memory

## Gotchas

- Doc-tests can produce false positives in discoverability checks; verify test results carefully when implementing documentation testing. _(spec: 01_project_setup, confidence: medium)_
- The `|| true` pattern can mask exit codes when used incorrectly in shell scripts; capture exit codes explicitly before applying error suppression. _(spec: 01_project_setup, confidence: high)_
- Kuksa Databroker 0.5.0 uses TCP port 55556 by default; ensure all references (docker-compose.yml, Makefile, configuration) are updated consistently to avoid port mismatches. _(spec: 02_data_broker, confidence: high)_
- Kuksa 0.5.0 API uses PublishValue instead of the older Actuate method for publishing values. _(spec: 02_data_broker, confidence: high)_
- ListMetadata in Kuksa 0.5.0 no longer includes a Path field, requiring test adjustments when upgrading from earlier versions. _(spec: 02_data_broker, confidence: high)_
- UDS directory must be created before infrastructure startup; this is typically handled in the Makefile to ensure the mount point exists before Docker containers attempt to use it. _(spec: 02_data_broker, confidence: high)_
- Docker-compose configuration must include all required services (e.g., NATS) or tests will fail with connection errors. _(spec: 01_project_setup, confidence: high)_
- Kuksa Databroker 0.5.0 strictly rejects type mismatches in signal read/write operations, which should be tested as an edge case in integration tests. _(spec: 02_data_broker, confidence: high)_
- Kuksa Databroker 0.5.0 does not implement the gRPC health check service, so health check endpoints cannot be relied upon in this version. _(spec: 02_data_broker, confidence: high)_

## Patterns

- Use the `|| true` pattern to capture exit codes in shell scripts without causing the script to fail when a command returns a non-zero exit code. _(spec: 01_project_setup, confidence: high)_
- Monorepo structure with multiple workspaces (Rust, Go) and shared proto definitions requires careful directory organization to maintain clear boundaries between crates/modules and enable code reuse. _(spec: 01_project_setup, confidence: high)_
- Go CLI applications can be organized into workspaces with multiple apps that share subcommand handling logic and common utilities. _(spec: 01_project_setup, confidence: high)_
- Root Makefile should orchestrate multiple language toolchains (Rust, Go) with targets for build, test, lint, clean, and infrastructure management. _(spec: 01_project_setup, confidence: high)_
- Configuration loading should support both file-based and embedded default fallback patterns to ensure robustness when external config files are unavailable. _(spec: 05_parking_fee_service, confidence: high)_
- Adapter metadata should be indexed during store constructor initialization to enable efficient lookup operations in downstream queries. _(spec: 05_parking_fee_service, confidence: high)_
- Geofence-dependent operations like FindOperatorsByLocation should initially wire to stub implementations when full geofencing logic is pending to allow incremental development. _(spec: 05_parking_fee_service, confidence: medium)_
- Integration tests for gRPC services should be organized into test groups covering happy path, property-based tests, and edge cases to ensure comprehensive coverage. _(spec: 02_data_broker, confidence: high)_
- Proto-generated gRPC client stubs must be added as dependencies alongside Go module dependencies when setting up integration test infrastructure. _(spec: 02_data_broker, confidence: high)_
- Ray-casting point-in-polygon algorithm is an effective method for geofence matching to determine if a location falls within a defined boundary. _(spec: 05_parking_fee_service, confidence: high)_
- Haversine distance calculation combined with distance-to-segment projection enables accurate proximity-based matching for geofence matching engines. _(spec: 05_parking_fee_service, confidence: high)_
- Docker Compose services should include health checks to enable wait-for-healthy logic in orchestration scripts, allowing proper synchronization of service startup. _(spec: 01_project_setup, confidence: high)_
- Makefile infra-up targets benefit from port polling with timeout logic (e.g., 30s) to wait for services to become healthy before proceeding with dependent operations. _(spec: 01_project_setup, confidence: high)_
- When adding Docker/Podman services, include health checks and restart policies in docker-compose.yml to ensure reliable container lifecycle management. _(spec: 02_data_broker, confidence: high)_
- Go 1.22 introduced enhanced ServeMux with pattern matching capabilities, enabling more sophisticated routing without external libraries. _(spec: 05_parking_fee_service, confidence: high)_
- Helper functions like writeJSON and writeError reduce boilerplate in HTTP handlers and improve consistency across endpoints. _(spec: 05_parking_fee_service, confidence: high)_
- Recovery middleware should be implemented to gracefully handle panics in HTTP handlers and prevent server crashes. _(spec: 05_parking_fee_service, confidence: high)_
- Configuration loading and HTTP server initialization should be centralized in main.go for clarity and maintainability. _(spec: 05_parking_fee_service, confidence: high)_
- Test scripts must isolate skeleton tests from intentionally-failing TDD tests to prevent false failures in CI/verification pipelines. _(spec: 01_project_setup, confidence: high)_
- Multi-language workspace test runners (Rust, Go) need explicit verification that all crates/modules are discovered and executed, not just a subset. _(spec: 01_project_setup, confidence: high)_
- A unified make test target should orchestrate multiple toolchain test runners to ensure comprehensive testing across the entire project. _(spec: 01_project_setup, confidence: high)_
- When initializing a Go project with external dependencies like NATS, create stub files for each logical module (model, auth, store, nats_client, handler, config) before writing comprehensive tests to ensure all imports resolve. _(spec: 06_cloud_gateway, confidence: high)_
- Organize test coverage across multiple dimensions: handler tests, integration tests with external systems (NATS), and property-based tests to catch edge cases and invariant violations. _(spec: 06_cloud_gateway, confidence: high)_
- VSS overlay files can be created with custom signals and mounted alongside standard VSS signal definitions in docker-compose for flexible signal configuration. _(spec: 02_data_broker, confidence: high)_
- Custom VSS signals can be organized in overlay files (e.g., Vehicle.Parking.SessionActive, Vehicle.Command.Door.*) for domain-specific extensions. _(spec: 02_data_broker, confidence: high)_
- Token validation middleware should extract bearer tokens and implement VIN-based authorization to control access to vehicle-specific endpoints. _(spec: 06_cloud_gateway, confidence: high)_
- Command and telemetry stores must be thread-safe and enforce terminal state protection to prevent invalid state transitions after completion. _(spec: 06_cloud_gateway, confidence: high)_
- Data models, authentication, storage, and REST handlers are distinct concerns that should be separated into different modules for maintainability. _(spec: 06_cloud_gateway, confidence: medium)_
- Kuksa Databroker supports dual listeners on both Unix Domain Socket (UDS) and TCP; UDS requires explicit --unix-socket flag and bind mount volume configuration in docker-compose.yml. _(spec: 02_data_broker, confidence: high)_
- When configuring dual protocol listeners in containerized environments, TCP and UDS paths should be tested separately as they have different failure modes and platform support characteristics. _(spec: 02_data_broker, confidence: medium)_
- Makefile should detect and use available container runtime (podman or docker) rather than hardcoding one, improving portability across development environments. _(spec: 01_project_setup, confidence: high)_
- When setting up a new Rust service module, establish failing spec tests across multiple modules (config, command, safety, integration) before implementation to define the contract. _(spec: 03_locking_service, confidence: high)_
- Use todo!() stubs in test implementations to create intentionally failing tests while keeping the project compilable and clippy-clean. _(spec: 03_locking_service, confidence: high)_
- Integration tests should compile successfully even when unit tests fail, indicating proper separation of test concerns and feature flag usage. _(spec: 03_locking_service, confidence: medium)_
- Use feature gates in Cargo.toml to conditionally compile integration tests, allowing unit tests to pass CI while integration tests remain stubbed with todo!(). _(spec: 04_cloud_gateway_client, confidence: high)_
- Write failing spec tests first using todo!() as placeholders, ensuring they compile and clippy passes before implementation begins. _(spec: 04_cloud_gateway_client, confidence: high)_
- Integration tests for data broker signal operations should cover both round-trip read/write and subscription delivery patterns across multiple signals. _(spec: 02_data_broker, confidence: high)_
- Edge case testing for signal operations should include non-existent signals, unset signals, type mismatches, and service health checks. _(spec: 02_data_broker, confidence: high)_
- When implementing gRPC clients with tonic over Unix Domain Sockets (UDS), configure the connection endpoint as 'unix:///path/to/socket' to properly establish IPC communication. _(spec: 03_locking_service, confidence: high)_
- Exponential backoff retry logic is essential for gRPC client operations to handle transient failures and gracefully recover from temporary service unavailability. _(spec: 03_locking_service, confidence: high)_
- Signal handling (SIGTERM/SIGINT) should be implemented in the main event loop to enable graceful shutdown and proper resource cleanup in long-running services. _(spec: 03_locking_service, confidence: high)_
- gRPC clients should implement connection recovery mechanisms to automatically re-establish failed subscriptions and resume signal monitoring after transient disconnections. _(spec: 03_locking_service, confidence: medium)_
- End-to-end verification for data broker includes infrastructure setup, running all tests, and infrastructure teardown as a complete validation cycle. _(spec: 02_data_broker, confidence: high)_
- Version-specific findings and API differences should be documented in dedicated errata files organized by specification and version. _(spec: 02_data_broker, confidence: high)_
- When implementing NATS client connections with TLS support, structure the configuration to include separate environment variables for URL and TLS enablement flag, allowing flexible deployment scenarios. _(spec: 04_cloud_gateway_client, confidence: high)_
- VIN-scoped NATS subjects should be implemented as helper functions in the client module to ensure consistent subject naming conventions across the application. _(spec: 04_cloud_gateway_client, confidence: high)_
- Application startup sequence should establish NATS connection before subscribing to commands, ensuring the connection is ready before handling incoming messages. _(spec: 04_cloud_gateway_client, confidence: high)_
- Configuration parsing should provide sensible defaults for optional settings like DATABROKER_UDS_PATH rather than requiring all environment variables to be explicitly set. _(spec: 04_cloud_gateway_client, confidence: high)_
- Shutdown signal handling should be integrated into the main startup sequence to allow graceful termination of connections and subscriptions. _(spec: 04_cloud_gateway_client, confidence: medium)_
- NATS client connection, command publishing, response subscription, and telemetry subscription can be implemented as a cohesive task group with comprehensive test coverage including integration tests and edge cases (unavailable service, invalid JSON). _(spec: 06_cloud_gateway, confidence: high)_
- When implementing NATS integration, test coverage should include both happy path scenarios (command relay, response subscription, telemetry reception) and failure modes (service unavailable, malformed data). _(spec: 06_cloud_gateway, confidence: high)_
- LoadConfig should support environment variables for configuration (HTTP_PORT, NATS_URL, KNOWN_VINS) to enable flexible deployment across environments without code changes. _(spec: 06_cloud_gateway, confidence: high)_
- Implement recovery middleware in HTTP servers to handle panic errors gracefully and prevent server crashes from unhandled exceptions. _(spec: 06_cloud_gateway, confidence: high)_
- Wire graceful shutdown handlers for servers to ensure clean resource cleanup and allow in-flight requests to complete before termination. _(spec: 06_cloud_gateway, confidence: high)_
- Implement both HTTP routes and NATS subscriptions in the same server process to support synchronous client requests and asynchronous telemetry/command response handling. _(spec: 06_cloud_gateway, confidence: medium)_
- gRPC over Unix Domain Sockets (UDS) requires careful connection management; exponential backoff retry logic is essential for reliability when connecting to external services like DATA_BROKER. _(spec: 04_cloud_gateway_client, confidence: high)_
- JSON parsing for command validation should include both structural validation (deserialization) and semantic validation (action enums, UUID formats) as separate concerns. _(spec: 04_cloud_gateway_client, confidence: high)_
- Task spawning with tokio requires explicit shutdown signal handling to ensure graceful termination of background processors in async pipelines. _(spec: 04_cloud_gateway_client, confidence: high)_
- Inbound message pipelines (NATS -> validate -> external service) benefit from clear separation of concerns across dedicated modules (command_processor, command, databroker_client) for maintainability and testability. _(spec: 04_cloud_gateway_client, confidence: high)_
- Command parsing should validate required fields and action types when deserializing from JSON, not just structure matching. _(spec: 03_locking_service, confidence: high)_
- Using dedicated constructor methods (success/failure) for response objects provides clearer intent and encapsulation than direct struct instantiation. _(spec: 03_locking_service, confidence: high)_
- Serialization (to_json) and deserialization (from_json) for command types should be implemented as symmetric pairs to prevent data loss or mismatches. _(spec: 03_locking_service, confidence: medium)_
- Integration tests for NATS-based systems should verify the full message flow cycle (REST-to-NATS-to-REST) to catch end-to-end routing and correlation issues. _(spec: 06_cloud_gateway, confidence: high)_
- When testing multi-tenant or multi-resource systems (e.g., multiple VINs), explicitly verify correct subject routing and response correlation to prevent cross-contamination bugs. _(spec: 06_cloud_gateway, confidence: high)_
- Integration tests should include failure scenario tests (e.g., NATS disconnect) with proper HTTP status code assertions (e.g., 503 for service unavailable) to ensure graceful degradation. _(spec: 06_cloud_gateway, confidence: high)_
- Concurrent access in NATS-based systems requires race condition testing via the -race flag to catch data races in message handling and response correlation logic. _(spec: 06_cloud_gateway, confidence: high)_
- Race detector is a critical part of the Go testing suite and should be run as part of checkpoint verification to catch concurrency issues. _(spec: 06_cloud_gateway, confidence: high)_
- When implementing concurrent message processing in Rust, use separate tokio tasks for different message types (commands, telemetry, responses) and coordinate them with shutdown signal handling. _(spec: 04_cloud_gateway_client, confidence: high)_
- Telemetry publishing from a message broker (DATA_BROKER) to a pub/sub system (NATS) requires a dedicated relay module that subscribes to signal changes and publishes them asynchronously. _(spec: 04_cloud_gateway_client, confidence: high)_
- Response relay modules should handle bidirectional message flow: consuming responses from a DATA_BROKER and relaying them back to the originating pub/sub system (NATS). _(spec: 04_cloud_gateway_client, confidence: high)_
- Multiple concurrent message processing tasks should be spawned and joined together with proper error handling and shutdown coordination to ensure graceful termination. _(spec: 04_cloud_gateway_client, confidence: high)_
- A complete command processing pipeline should follow a linear flow: parse → validate → safety check → execute → respond, with each stage building on the previous one's output. _(spec: 03_locking_service, confidence: high)_
- Safety constraint checks (speed and door ajar validation) should be implemented as a separate validation stage before command execution to ensure system invariants are maintained. _(spec: 03_locking_service, confidence: high)_
- Lock/unlock operations should be split into two responsibilities: state writing (persistence) and response publishing (communication), keeping state mutations separate from side effects. _(spec: 03_locking_service, confidence: high)_
- Integration tests require exposing internal modules via lib.rs to allow test access to implementation details that wouldn't normally be public API. _(spec: 03_locking_service, confidence: high)_
- Comprehensive integration test coverage should span happy paths, constraint validation, error handling, response format validation, and property-based invariants to achieve thorough specification coverage. _(spec: 03_locking_service, confidence: high)_
- Test client utilities like DatabrokerClient need methods for establishing connections (TCP) and configuring signals before integration tests can execute realistic scenarios. _(spec: 03_locking_service, confidence: medium)_
- gRPC servers in Rust using tonic are initialized via Server::builder() pattern, which provides a fluent interface for configuring the server before binding. _(spec: 07_update_service, confidence: high)_
- Configuration loading should support both TOML file defaults and environment variable overrides, using a consistent prefix (e.g., UPDATE_SERVICE_) for env vars to organize settings by service. _(spec: 07_update_service, confidence: high)_
- Returning UNIMPLEMENTED status for gRPC methods during early development stages is a valid placeholder strategy that allows server wiring to be verified without full business logic. _(spec: 07_update_service, confidence: medium)_
- PodmanOciPuller struct uses CLI-based methods (pull_image, remove_image) for container image operations rather than programmatic APIs. _(spec: 07_update_service, confidence: high)_
- Unit tests for checksum verification and struct construction were added as part of the OCI pull task implementation. _(spec: 07_update_service, confidence: high)_
- Use tokio::process::Command for executing container CLI commands (podman run, stop, remove, status) in async Rust contexts rather than blocking process calls. _(spec: 07_update_service, confidence: high)_
- Implement a dedicated helper function like parse_container_status to encapsulate parsing logic for container state, improving testability and maintainability. _(spec: 07_update_service, confidence: high)_
- Use trait-based design (ContainerRuntime trait with PodmanRuntime struct) to abstract container runtime implementations, enabling flexibility and testability. _(spec: 07_update_service, confidence: high)_
- State machine transitions should be explicitly enforced and validated according to specification requirements to prevent invalid state changes. _(spec: 07_update_service, confidence: high)_
- AdapterManager should enforce single-adapter constraints at the business logic layer to maintain system integrity. _(spec: 07_update_service, confidence: high)_
- Checksum verification should be integrated into adapter installation/removal operations to ensure data integrity. _(spec: 07_update_service, confidence: high)_
- gRPC endpoints should delegate to a centralized manager and map internal errors to appropriate gRPC error codes for consistent error handling. _(spec: 07_update_service, confidence: high)_
- When setting up a Rust crate with protobuf definitions and gRPC, organize modules into logical domains (session, grpc, operator, broker) to separate concerns between state management, service handlers, and external integrations. _(spec: 08_parking_operator_adaptor, confidence: high)_
- State machine transitions should be tested explicitly with spec tests before implementation; this clarifies expected behavior and validates the design early. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Serde serialization/deserialization for operator models should be validated independently; models can be correct while gRPC handlers or REST client integration remains unimplemented. _(spec: 08_parking_operator_adaptor, confidence: medium)_
- gRPC service implementations require explicit error code handling for each RPC method to ensure proper client-side error interpretation. _(spec: 08_parking_operator_adaptor, confidence: high)_
- State machine transitions (idle→starting→active→stopping) with error handling require explicit handling of all state combinations to prevent invalid transitions. _(spec: 08_parking_operator_adaptor, confidence: high)_
- gRPC server wiring in main.rs requires careful integration of service implementations with the server startup logic. _(spec: 08_parking_operator_adaptor, confidence: medium)_
- Integration tests should include smoke tests covering end-to-end flows like connection establishment, subscriptions, retry logic, and error handling to validate real-world behavior beyond unit test coverage. _(spec: 04_cloud_gateway_client, confidence: high)_
- Task group implementations should include both functional tests and property-based tests (e.g., invariant verification) to ensure correctness of constraints like single-adapter requirements. _(spec: 07_update_service, confidence: high)_
- Background timer-based operations like adapter offloading should be configurable (e.g., check interval) to allow flexibility in deployment scenarios and testing. _(spec: 07_update_service, confidence: high)_
- State machine enforcement, single-adapter constraints, and automatic offloading are critical components that must be validated during checkpoint verification for update service implementations. _(spec: 07_update_service, confidence: high)_
- BrokerSubscriber should implement exponential backoff retry logic when subscribing to Kuksa VAL gRPC events to handle transient connection failures gracefully. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Use async-stream and tokio-stream dependencies for handling asynchronous streaming operations in Rust, particularly when working with gRPC subscriptions. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Separate subscriber and publisher concerns into distinct modules (BrokerSubscriber and BrokerPublisher) to maintain single responsibility and testability when working with message brokers. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Rust sensor crates benefit from inline test modules within the same file as implementation for unit testing, keeping tests close to code. _(spec: 09_mock_apps, confidence: high)_
- Go HTTP servers should have handler stubs and dedicated handler_test.go files to systematically cover endpoint behavior and error cases. _(spec: 09_mock_apps, confidence: high)_
- CLI applications with multiple subcommands should use a cmd/ package dispatch pattern to organize command handlers and make testing individual commands easier. _(spec: 09_mock_apps, confidence: high)_
- Mock app components should have failing spec test scaffolding created upfront to drive implementation against explicit test specifications. _(spec: 09_mock_apps, confidence: medium)_
- Integration tests for operator adaptors can effectively use mock HTTP servers (axum) to simulate operator behavior, enabling comprehensive testing of client timeout handling, endpoint contracts, and error scenarios without external dependencies. _(spec: 08_parking_operator_adaptor, confidence: high)_
- REST client implementations for external operators should enforce explicit timeouts (e.g., 5s) at the client level to prevent indefinite hangs and ensure graceful degradation when operator services are unreachable. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Rust CLI tools can publish VSS signals to DATA_BROKER using tonic gRPC client with kuksa.val.v2 protocol via the PublishValue method. _(spec: 09_mock_apps, confidence: high)_
- Mock sensor implementations should parse CLI arguments, establish gRPC connections to DATA_BROKER, and publish signals in a consistent pattern across multiple sensor types. _(spec: 09_mock_apps, confidence: high)_
- Integration tests for REST contract validation should cover both happy path cycles (start/stop) and status queries to ensure the adapter correctly implements the REST interface specification. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Error handling tests for unreachable operators should cover multiple failure points: operator unreachable during start, during stop, and recovery scenarios to ensure robust failure handling. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Lifecycle tests should include edge cases like full lifecycle continuation after a stop operation fails, ensuring the adapter maintains correct state through error scenarios. _(spec: 08_parking_operator_adaptor, confidence: medium)_
- In-memory SessionStore should implement Create, Stop, and List methods as core operations for managing session lifecycle in a mock server. _(spec: 09_mock_apps, confidence: high)_
- Mock REST servers benefit from CLI flag support (e.g., -port) for flexible deployment across different test environments and configurations. _(spec: 09_mock_apps, confidence: high)_
- Integration tests for gRPC services should cover both idempotency scenarios (double operations) and error code validation to ensure robust state management and proper error handling. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Manual override flows combined with autonomous flows require explicit state-signal consistency validation across full cycles to catch race conditions and state machine violations. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Parking operator adaptor implementations need comprehensive testing of lock/unlock operations including double-lock idempotency to prevent resource contention issues. _(spec: 08_parking_operator_adaptor, confidence: medium)_
- When building a CLI application with multiple subcommands, organize shared functionality into internal packages (config, output, restclient, grpcclient) to avoid duplication and maintain consistency across commands. _(spec: 09_mock_apps, confidence: high)_
- gRPC stubs should be generated from proto definitions as part of the build process rather than manually written, ensuring consistency between client and server contract definitions. _(spec: 09_mock_apps, confidence: high)_
- CLI applications need to support multiple communication protocols (REST and gRPC) simultaneously; abstract these behind common client interfaces to allow flexible switching between backends. _(spec: 09_mock_apps, confidence: medium)_
- When implementing CLI applications with REST communication, organize code into internal packages by responsibility (config, output, restclient) to maintain separation of concerns and testability. _(spec: 09_mock_apps, confidence: high)_
- Bearer token support should be implemented as a dedicated concern within the restclient package rather than scattered across command handlers. _(spec: 09_mock_apps, confidence: high)_
- CLI subcommands (lock, unlock, status) that interact with external services should be testable independently, requiring abstraction of the HTTP client layer. _(spec: 09_mock_apps, confidence: high)_
- A complete checkpoint verification includes running all test suites across multiple language ecosystems (Rust, Go), executing all linters (cargo clippy, go vet), and confirming all binaries build successfully. _(spec: 09_mock_apps, confidence: high)_

## Decisions

- SHA-256 is the chosen algorithm for OCI image checksum verification in this implementation. _(spec: 07_update_service, confidence: high)_

## Conventions

- Use a structured naming convention for test cases (e.g., TS-02-1 for happy path, TS-02-P1 for property tests, TS-02-E1 for edge cases) to improve test traceability and specification alignment. _(spec: 02_data_broker, confidence: high)_
- Comprehensive test coverage (7 geofence tests + store location tests) is critical for validating spatial matching logic to prevent edge cases in real-world deployments. _(spec: 05_parking_fee_service, confidence: high)_
- Validate container startup/teardown and run corresponding health tests after deploying new services to catch integration issues early. _(spec: 02_data_broker, confidence: high)_
- Health check endpoints are a standard convention for monitoring service availability in REST APIs. _(spec: 05_parking_fee_service, confidence: high)_
- Writing failing spec tests before implementation (red phase of TDD) requires ensuring all test files compile successfully so that failures are semantic rather than syntactic. _(spec: 06_cloud_gateway, confidence: high)_
- REST API handlers should include health checks, standardized JSON error responses, and explicit 404 handling for missing resources. _(spec: 06_cloud_gateway, confidence: high)_
- UDS tests should be skipped on macOS when using Podman due to VM boundary limitations; UDS functionality can still be verified via container logs even when tests are skipped. _(spec: 02_data_broker, confidence: high)_
- Test scripts must use consistent port numbers across all configurations to avoid flaky tests and connection failures. _(spec: 01_project_setup, confidence: high)_
- Add required dependencies (serde, serde_json) and feature flags (integration) to Cargo.toml when introducing serialization and feature-gated test modules. _(spec: 03_locking_service, confidence: high)_
- Organize test modules by concern (config.rs for env var parsing, command.rs for validation, integration.rs for feature-gated tests) to maintain clarity and enable selective test execution. _(spec: 04_cloud_gateway_client, confidence: high)_
- Structure test specifications with clear naming conventions (e.g., TS-04-2 for config tests, TS-04-E1 for error cases) to map directly to requirements and enable traceability. _(spec: 04_cloud_gateway_client, confidence: medium)_
- Version-specific behaviors and limitations (e.g., missing gRPC health check in 0.5.0) should be documented in an errata section for test specifications. _(spec: 02_data_broker, confidence: high)_
- Environment variable parsing with sensible defaults should be centralized in a dedicated configuration module (e.g., config.rs) rather than scattered throughout the codebase. _(spec: 03_locking_service, confidence: high)_
- Requirements coverage should be tracked with explicit documentation of passing tests or justified coverage gaps for each requirement. _(spec: 02_data_broker, confidence: high)_
- Implementing new messaging functionality alongside existing handlers and properties requires careful test isolation to ensure all 25 tests pass without regression. _(spec: 06_cloud_gateway, confidence: high)_
- Use race detector (go test -race) and go vet as part of validation to catch concurrency issues and common coding mistakes early. _(spec: 06_cloud_gateway, confidence: high)_
- Replacing todo!() stubs with actual implementations should be validated with comprehensive unit tests covering both success and failure cases. _(spec: 03_locking_service, confidence: high)_
- A complete checkpoint should verify: all tests pass including race detector, go vet passes, binary builds successfully, and all Definition of Done criteria from design.md are satisfied. _(spec: 06_cloud_gateway, confidence: high)_
- Design.md serves as the authoritative source for Definition of Done criteria that must be verified before marking a task group complete. _(spec: 06_cloud_gateway, confidence: high)_
- Rust clippy warnings should be eliminated as part of code quality validation, alongside unit test coverage, to catch potential issues early in the development cycle. _(spec: 03_locking_service, confidence: high)_
- Proto compilation end-to-end verification should be part of the implementation checklist to ensure generated Rust code from .proto files integrates correctly with the server. _(spec: 07_update_service, confidence: high)_
- Co-locate comprehensive unit tests in a dedicated test module (container_test.rs) covering all container operations to ensure reliability before integration. _(spec: 07_update_service, confidence: high)_
- A healthy test setup distinguishes between expected stub failures (incomplete implementation) and passing model tests (correct data handling), helping identify which layers need work. _(spec: 08_parking_operator_adaptor, confidence: medium)_
- Configuration modules using environment variables should provide sensible defaults to avoid runtime errors from missing configuration. _(spec: 08_parking_operator_adaptor, confidence: high)_
- Comprehensive test coverage should span unit tests, integration tests, and manual smoke tests to verify both individual components and requirement fulfillment. _(spec: 04_cloud_gateway_client, confidence: high)_
- A complete checkpoint verification includes 42 passing tests, zero clippy warnings, successful proto compilation, and validation of all Definition of Done criteria covering gRPC server, InstallAdapter flow, state streaming, adapter management, checksum verification, single-adapter constraint, automatic offloading, and state machine enforcement. _(spec: 07_update_service, confidence: high)_
- When integrating with Kuksa VAL, the kuksa_proto module needs to be explicitly added to the crate to support gRPC protocol buffer code generation and message types. _(spec: 08_parking_operator_adaptor, confidence: high)_
- DATA_BROKER state writes should target specific vehicle signals like Vehicle.Parking.SessionActive for parking operator state synchronization. _(spec: 08_parking_operator_adaptor, confidence: medium)_
- Comprehensive test coverage for CLI apps should include tests for subcommand dispatch, REST endpoints, gRPC methods, missing flags, and service unreachable errors. _(spec: 09_mock_apps, confidence: high)_
- Clippy warnings should be resolved and unit tests should achieve full pass rates as quality gates before considering sensor implementations complete. _(spec: 09_mock_apps, confidence: high)_
- HTTP handler endpoints should follow resource-oriented naming conventions (POST /parking/start, POST /parking/stop, GET /parking/status) mapping to session lifecycle operations. _(spec: 09_mock_apps, confidence: high)_
- Flag parsing in CLI subcommands should be implemented consistently across all commands to provide a uniform user experience and reduce cognitive load. _(spec: 09_mock_apps, confidence: high)_
- A complete CLI implementation should validate both functionality (all tests pass) and code quality (go vet clean) before considering the task complete. _(spec: 09_mock_apps, confidence: high)_
- Definition of Done items should be explicitly documented in design.md and systematically verified during checkpoint completion to ensure specification compliance. _(spec: 09_mock_apps, confidence: high)_

## Anti-Patterns

- Shared mutable state (like DATA_BROKER) across concurrent tests causes flakiness; tests need isolation or synchronization mechanisms to prevent state leakage between parallel executions. _(spec: 04_cloud_gateway_client, confidence: high)_

## Fragile Areas

- Test infrastructure scripts need careful handling of exit code propagation to avoid silently ignoring test failures. _(spec: 01_project_setup, confidence: high)_
- Container runtime detection in infrastructure scripts must handle cases where the runtime is unavailable or not installed, requiring robust error checking before attempting container operations. _(spec: 01_project_setup, confidence: high)_
- Go skeleton scripts need proper exit code handling to signal success/failure correctly to calling processes. _(spec: 01_project_setup, confidence: high)_
- Wiring background operations (like OffloadTimer) into main.rs requires careful integration to ensure the component lifecycle is managed correctly alongside other services. _(spec: 07_update_service, confidence: medium)_
- Event loop wiring that bridges external event sources (DATA_BROKER) to internal state machines requires careful synchronization to ensure operator calls are triggered correctly and idempotency is maintained across repeated lock/unlock events. _(spec: 08_parking_operator_adaptor, confidence: high)_
- gRPC service integration with event publishers (DATA_BROKER) requires careful coordination between incoming events and outgoing operator commands to avoid cascading failures and maintain service reliability. _(spec: 08_parking_operator_adaptor, confidence: medium)_
