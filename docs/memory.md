# Agent-Fox Memory

_260 facts | last updated: 2026-04-17_

## Gotchas

- When a perpendicular foot falls outside a segment's endpoints, return the distance to the nearer endpoint instead of extrapolating; this is critical for correct polygon corner handling. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: Distance-to-segment calculations for geographic data should …
- In Rust gRPC clients, clone the client before calling async methods (e.g., `self.client.clone().get(request).await`) to avoid holding exclusive borrows across await points. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Use `tonic::include_proto!()` macro to include generated pro…
  - effect: When subscribing to streaming gRPC responses, spawn a backgr…
- Datapoint proto messages require setting timestamp field explicitly (even if 0) and wrapping values in the appropriate DatapointValue enum variant (e.g., BoolValue, StringValue, FloatValue). _(spec: 02_data_broker, confidence: 0.90)_
  - cause: For best-effort JSON field extraction (e.g., extracting comm…
- ValClient from tonic-generated code must be cloned before each RPC call (client.clone().get/set/subscribe) because it wraps a shared Channel and cloning is cheap. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: Use tonic::include_proto!() macro to include generated proto…
  - effect: When implementing subscription streaming in tonic, spawn a b…
- Use biased; in tokio::select! to ensure shutdown signals are checked first, preventing new command processing after shutdown is initiated. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Use tokio::select! with biased; to prioritize shutdown signa…
  - cause: Register SIGTERM handler using tokio::signal::unix::signal(S…
- tonic-build must be added as a build-dependency (not a regular dependency) to compile .proto files during the build phase via build.rs. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Use tonic-build in build.rs to compile Protocol Buffer defin…
  - effect: Use tonic::include_proto!() macro to include generated proto…
- Makefile targets for proto, make, and test-setup may already exist from previous task groups; verify before re-implementing to avoid duplication. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Setup tests should use exec.LookPath to conditionally skip t…
- Empty command_id string passes JSON deserialization but must be rejected during validation (semantic vs. syntactic validation separation). _(spec: 03_locking_service, confidence: 0.90)_
  - cause: When a required field is missing from a JSON payload, treat …
  - effect: Success responses must NOT include a reason field; failure r…
- When a required field is missing from a JSON payload, treat InvalidJson and InvalidCommand as distinct errors: InvalidJson for syntax errors (discard without response), InvalidCommand for missing/invalid fields (respond only if command_id is extractable). _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Empty command_id string passes JSON deserialization but must…
- Skeleton implementations that print version info on no arguments (spec 01) are fundamentally incompatible with requirements to reject missing required flags (spec 09), causing pre-existing test failures that will be resolved when full implementations replace skeletons. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: Skeleton binaries should print a version string (e.g., 'comp…
  - cause: Skeleton binaries must reject unknown flags (arguments start…
- Spec 01 skeleton behavior (exit 0 with no args) is fundamentally incompatible with Spec 09 full implementation requirements (exit 1 with missing required flags); this is documented as expected and resolved during spec 09 task group 2. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Skeleton binaries should print their name and version (v0.1.…
- When skeleton binaries exit with code 0 and print version info, they may fail pre-existing tests written for full implementations that expect exit code 1 with required argument validation; this is expected divergence, not a bug. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Skeleton binaries should print their name and version (e.g.,…
  - cause: Skeleton binaries must reject unknown flags (arguments start…
- In Go module test setups, some tests may pass immediately (like directory existence tests) simply because the test module itself was created, while others fail until the actual project structure is built out. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: When setting up test suites for a project structure specific…
- Trait methods like BrokerClient are declared async and accept async_fn_in_trait; this requires Rust 1.75+ or a nightly feature flag for compatibility. _(spec: 03_locking_service, confidence: 0.60)_
- In a RED-phase test implementation, use exit code 0 for stub binaries that should fail tests expecting non-zero exits; tests will fail at runtime as required even though code compiles cleanly. _(spec: 09_mock_apps, confidence: 0.60)_
  - effect: Structure multi-language test suites (Rust + Go) with separa…

## Patterns

- Distance-to-segment calculations for geographic data should use a locally flat coordinate system (scaled by cos(lat)) for projection math, then convert back to lat/lon for final Haversine distance to maintain accuracy. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: When a perpendicular foot falls outside a segment's endpoint…
  - cause: Degenerate cases (empty polygon, single-vertex polygon, zero…
  - effect: Use flat-Earth scaling factors of 111,320 metres per degree …
  - effect: Zone matching uses an OR condition: a point matches if it's …
- The ray-casting algorithm for PointInPolygon uses longitude as X-axis and latitude as Y-axis, treating the polygon as implicitly closed and rejecting degenerate polygons with fewer than 3 vertices. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: Zone matching uses an OR condition: a point matches if it's …
- Handler implementations should validate input (e.g., lat/lon validation) and return appropriate HTTP status codes (e.g., 404 for missing resources) before processing business logic. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: feat: implement main package for cloud-gateway (task group 5…
- Use slog for structured logging during startup to provide visibility into application initialization and configuration loading. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: feat: implement mock PARKING_OPERATOR serve subcommand (task…
  - effect: feat: implement main package for cloud-gateway (task group 5…
- Use http.Server.Shutdown for graceful shutdown handling of SIGTERM/SIGINT signals, allowing in-flight requests to complete before termination. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: feat: implement mock PARKING_OPERATOR serve subcommand (task…
  - effect: feat: implement main package for cloud-gateway (task group 5…
- Go 1.22 ServeMux supports direct HTTP method routing (e.g., GET /path) without third-party routers, eliminating the need for external routing libraries in simple cases. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: feat: implement mock PARKING_OPERATOR serve subcommand (task…
  - effect: feat: implement main package for cloud-gateway (task group 5…
- DistanceToPolygonEdge requires combining flat-Earth segment projection with endpoint clamping and then applying Haversine formula for final distance calculation. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: HaversineDistance formula calculates great-circle distance b…
  - effect: Zone matching logic should first check polygon containment, …
- HaversineDistance formula calculates great-circle distance between two geographic coordinates, accounting for Earth's curvature. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: DistanceToPolygonEdge requires combining flat-Earth segment …
- PointInPolygon can be implemented using ray casting algorithm to determine if a point lies within a polygon boundary. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Zone matching logic should first check polygon containment, …
- Platform-conditional signal handling: use `#[cfg(unix)]` for POSIX signals; provide a fallback using `futures::future::pending()` on non-Unix platforms. _(spec: 07_update_service, confidence: 0.90)_
  - effect: Use `tokio::select!` with `biased` to prioritize SIGTERM/SIG…
- Implement resubscription logic with a bounded attempt counter to prevent infinite retry loops on persistent stream failures; exit cleanly after max attempts exceeded. _(spec: 07_update_service, confidence: 0.90)_
  - cause: When subscribing to streaming gRPC responses, spawn a backgr…
  - cause: Use `tokio::select!` with `biased` to prioritize SIGTERM/SIG…
- Use `tokio::select!` with `biased` to prioritize SIGTERM/SIGINT shutdown signals over business logic loops, ensuring graceful shutdown is processed first. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Platform-conditional signal handling: use `#[cfg(unix)]` for…
  - cause: In async Rust services, use `#[tokio::main]` attribute macro…
  - effect: Implement resubscription logic with a bounded attempt counte…
- When subscribing to streaming gRPC responses, spawn a background task with `tokio::spawn()` to forward messages to an mpsc channel, allowing non-blocking receipt by the caller. _(spec: 07_update_service, confidence: 0.90)_
  - cause: In Rust gRPC clients, clone the client before calling async …
  - cause: In async Rust services, use `#[tokio::main]` attribute macro…
  - effect: Implement resubscription logic with a bounded attempt counte…
- Use `tonic::include_proto!()` macro to include generated protobuf code in a module, and suppress clippy warnings on the generated module with `#[allow(clippy::enum_variant_names)]`. _(spec: 07_update_service, confidence: 0.90)_
  - effect: In Rust gRPC clients, clone the client before calling async …
  - replaces: Use tonic::include_proto!() macro to include generated protobuf code in a module…
- On Unix systems, use tokio::signal::unix::signal(SignalKind::terminate()) to register SIGTERM handlers; wrap platform-specific code in #[cfg(unix)] and #[cfg(not(unix))] blocks. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: In async Rust, use tracing_subscriber::fmt().with_env_filter…
  - effect: When using futures::future::pending() to block indefinitely …
- Use tokio::select! with biased; to prioritize certain branches (e.g., shutdown signal) over others in concurrent operations; reset state flags (like resubscription attempts) on successful processing. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: Exponential backoff retry logic for gRPC connection attempts…
  - effect: In async Rust, use tracing_subscriber::fmt().with_env_filter…
- When implementing subscription streaming in tonic, spawn a background tokio task to forward stream messages to a channel; handle receiver closure (tx.send().is_err()) to detect when the consumer has disconnected. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: ValClient from tonic-generated code must be cloned before ea…
  - effect: Exponential backoff retry logic for gRPC connection attempts…
- Register SIGTERM handler using tokio::signal::unix::signal(SignalKind::terminate()) and use a watch channel to broadcast shutdown signal to multiple async tasks. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Use tokio::select! with biased; to prioritize shutdown signa…
  - effect: Use biased; in tokio::select! to ensure shutdown signals are…
- Implement idempotent state writes by checking current state before calling set_bool, skipping the write if the target state is already set to avoid redundant gRPC calls. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: For gRPC subscription streams in Rust, clone the client befo…
- Use tokio::select! with biased; to prioritize shutdown signals over other operations, and use 'outer: and 'inner: loop labels to control resubscription flow after stream interruption. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: For gRPC subscription streams in Rust, clone the client befo…
  - cause: Register SIGTERM handler using tokio::signal::unix::signal(S…
  - effect: Use biased; in tokio::select! to ensure shutdown signals are…
- For gRPC subscription streams in Rust, clone the client before calling subscribe(), spawn a background task to forward messages to an mpsc channel, and detect stream interruption when recv() returns None. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Implement exponential backoff retry logic with hardcoded del…
  - cause: Implement idempotent state writes by checking current state …
  - effect: Use tokio::select! with biased; to prioritize shutdown signa…
- Use tonic::include_proto!() macro to include generated protobuf code, wrapping it in a module to manage namespaces and allow selective allow() attributes for clippy lints. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: tonic-build must be added as a build-dependency (not a regul…
- Store initialization should create multiple indexes (by ID and by zone_id) to support efficient lookups across different query patterns. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: LoadConfig should read JSON configuration, fall back to Defa…
- LoadConfig should read JSON configuration, fall back to DefaultConfig only on file-not-exist errors, and propagate invalid JSON errors to the caller. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: DefaultConfig should provide sensible demo values (e.g., Mun…
  - effect: Store initialization should create multiple indexes (by ID a…
- Implement graceful shutdown by handling SIGTERM/SIGINT signals to cleanly drain async tasks and close connections. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Initialize distributed tracing early in async main before sp…
- Add resubscription retry logic (up to 3 attempts) in subscription loops to recover from transient gRPC failures. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Implement exponential backoff with a configurable attempt li…
  - cause: Use mpsc channels for subscription event streaming from asyn…
- Implement idempotent state management in command processing to safely handle duplicate lock/unlock operations. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Use mpsc channels for subscription event streaming from asyn…
- Use mpsc channels for subscription event streaming from async gRPC subscriptions to consumer loops. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Implement idempotent state management in command processing …
  - effect: Add resubscription retry logic (up to 3 attempts) in subscri…
- Implement exponential backoff with a configurable attempt limit (5 attempts used here) for gRPC broker connection resilience. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Use tonic-build in build.rs to compile Protocol Buffer defin…
  - effect: Add resubscription retry logic (up to 3 attempts) in subscri…
- Use tonic-build in build.rs to compile Protocol Buffer definitions at build time for gRPC service generation. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: tonic-build must be added as a build-dependency (not a regul…
  - effect: Implement exponential backoff with a configurable attempt li…
- Async-nats operations should be instrumented with tracing at key points (connect, subscribe, publish) for observability in production systems. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Exponential backoff for connection retry should use realisti…
  - cause: Cargo.toml should include both async-nats and tokio with tra…
- Exponential backoff for connection retry should use realistic delays (1s, 2s, 4s, 8s) over 5 attempts to balance responsiveness with avoiding overwhelming the service. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Async-nats operations should be instrumented with tracing at…
- When testing multiple operators in a single zone, verify that GetOperatorsByZoneIDs returns all operators for that zone, not just the first one. _(spec: 05_parking_fee_service, confidence: 0.90)_
- Property-based tests should verify invariants across multiple inputs (e.g., coordinate ranges, zone subsets) to ensure implementation correctness beyond individual test cases. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Table-driven tests with pre-verified test cases are preferre…
  - cause: Helper functions in test code should be marked with t.Helper…
- Graceful shutdown tests should send SIGTERM, wait for exit with a timeout, and verify exit code 0, falling back to SIGKILL if the service doesn't exit in time. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Integration tests for service startup should use context tim…
- Integration tests for service startup should use context timeouts (e.g., 5 seconds) with a polling loop on the /health endpoint to verify the service is ready, avoiding race conditions. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Graceful shutdown tests should send SIGTERM, wait for exit w…
- When loading config from a JSON file with invalid JSON, LoadConfig should return a non-nil error rather than a default config, distinguishing between missing files and corrupt files. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: When a config file does not exist, LoadConfig should return …
- Coordinate validation should enforce latitude range [-90, 90] and longitude range [-180, 180], returning HTTP 400 with error message "invalid coordinates" for out-of-range values. _(spec: 05_parking_fee_service, confidence: 0.90)_
- Operator lookup responses should exclude sensitive adapter metadata (image_ref, checksum, version) and require a separate /operators/{id}/adapter endpoint to retrieve it. _(spec: 05_parking_fee_service, confidence: 0.90)_
- HTTP handlers should return JSON error responses in a consistent format {"error":"<message>"} with appropriate status codes (400 for validation, 404 for not found). _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: All HTTP endpoints must set Content-Type: application/json h…
- When a config file does not exist, LoadConfig should return a default configuration (e.g., Munich demo data) with no error rather than failing, providing graceful fallback behavior. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: When loading config from a JSON file with invalid JSON, Load…
- Stub implementations should clearly document that they are stubs with a comment indicating which task group contains the full implementation, allowing tests to fail predictably until that group is reached. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: The Makefile GO_PASSING_MODULES list must be updated to excl…
- A complete build/test/lint verification cycle should include: make clean && make build && make test, make proto && make build-go, make test-setup (with skeleton version string tests), and make check (linter verification). _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Skeleton version string tests (TestRustSkeletonVersionString…
- Implement state update methods to return `Option<T>` (None for unchanged values, Some for changed values) to suppress duplicate updates and provide clear signaling of whether state actually changed. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Use `skip_serializing_if` attribute in serde to conditionall…
- Use `skip_serializing_if` attribute in serde to conditionally omit fields from JSON serialization based on whether they have been set, useful for telemetry payloads where not all fields are always populated. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Implement state update methods to return `Option<T>` (None f…
- Setup tests should use exec.LookPath to conditionally skip toolchain-dependent tests when tools (cargo, go, protoc) are absent from PATH, rather than failing hard. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Makefile targets for proto, make, and test-setup may already…
- Parse raw bytes as serde_json::Value before field-level validation to enable structured error reporting for individual command fields. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Command payload validation should enforce command_id as non-…
- Bearer token validation should check for exact 'Bearer <token>' format in the Authorization header, not just token presence. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Command payload validation should enforce command_id as non-…
- Root Makefile should include targets for proto compilation, infrastructure setup (infra-up/infra-down), and test setup to maintain consistent build workflows. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Project setup verification should include comprehensive test…
- VSS signal customization is done via a vss-overlay.json file that extends the base Vehicle Signal Specification. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Project setup verification should include comprehensive test…
- Environment variable configuration should distinguish between mandatory parameters (like VIN, which should exit with a specific error if absent) and optional parameters with sensible defaults (NATS_URL, DATABROKER_ADDR, BEARER_TOKEN). _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Use clippy with -D warnings flag to enforce strict linting d…
  - cause: Separating configuration loading (from_env) from data model …
- Use MockBrokerClient with RefCell for unit tests (not Send/Sync) under single-threaded tokio::test; switch to GrpcBrokerClient in integration tests with multi-threaded runtime. _(spec: 03_locking_service, confidence: 0.90)_
- Lock operations are idempotent: only call set_bool when state actually changes; repeated commands with the same action should not write state if already in that state. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Unlock commands bypass all safety checks (speed, door state)…
- Property-based tests (proptest) must run on a custom single-threaded tokio runtime to avoid Send/Sync conflicts with RefCell-based mocks. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: MockBrokerClient uses RefCell and is not Send/Sync; it only …
  - replaces: Mock test objects using RefCell for interior mutability may not be Send/Sync, bu…
- Lock and unlock operations are idempotent: if the door is already in the target state, return success without calling set_bool (REQ-4.E1, REQ-4.E2). _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Unlock commands bypass all safety checks (speed and door sta…
  - effect: Success responses must omit the reason field; failure respon…
- Lock and unlock operations must be idempotent: setting the same state multiple times should only call set_bool once, not repeatedly (REQ-4.E1, REQ-4.E2). _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Unlock commands bypass all safety checks (speed and door sta…
  - effect: Success responses must omit the reason field entirely (using…
- When implementing a Rust crate with comprehensive testing, organize tests across multiple specialized modules (unit tests, property tests) with stub implementations (todo!()) to establish test-first development before implementation. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Document specification contradictions (e.g., REQ-2.3 vs REQ-…
  - effect: Use property-based testing (proptest) in addition to unit te…
- Mark integration or slow-running property tests with #[ignore] during initial implementation to keep fast feedback loops while maintaining test completeness. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Squashed commit of the following:

commit 8322ce4a4ed9be0e62…
  - effect: Squashed commit of the following:

commit 5d3cb1a5e58631f545…
- When implementing specs with failing tests first, stub implementations with todo!() panics to satisfy the test framework while clearly indicating incomplete work. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Squashed commit of the following:

commit a6c1d7f2851c0b90dd…
- Skeleton binaries must reject unknown flags (arguments starting with '-') by writing a usage message to stderr and exiting with code 1. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Skeleton implementations that print version info on no argum…
  - replaces: Skeleton binaries must reject unknown flags (arguments starting with '-') by pri…
- Skeleton binaries should print a version string (e.g., 'component-name v0.1.0') to stdout when invoked with no arguments and exit with code 0. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Skeleton implementations that print version info on no argum…
  - replaces: Skeleton binaries should print their name and version (e.g., 'component-name v0.…
- When defining multiple related gRPC services (VAL, UpdateService, AdapterService, CommandRelay), organize proto files by service domain in separate files rather than combining them. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Proto3 files should include package declarations and go_pack…
  - effect: Proto files should be validated with protoc compiler (e.g., …
- Rust skeleton binaries should use a for loop over std::env::args().skip(1) to check for flags starting with '-' and reject them with eprintln! and std::process::exit(1). _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Skeleton binaries must reject unknown flags (arguments start…
- Conflicting requirements between specification versions (spec 01 skeleton behavior vs spec 09 full implementation) should be documented in erratum files with clear explanation of root cause and resolution timing. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Skeleton binaries should print their name and version (e.g.,…
  - cause: Skeleton binaries must reject unknown flags (arguments start…
- Each Rust crate should include a placeholder #[test] it_compiles test to verify basic compilation. _(spec: 01_project_setup, confidence: 0.90)_
- Test-driven setup implementation allows early validation of directory structure and workspace configuration before dependent infrastructure tasks (like Makefile targets) are implemented. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: A monorepo with multiple language ecosystems (Rust Cargo, Go…
  - effect: Binary targets in Rust crates (e.g., 3 binaries in mock-sens…
- A monorepo with multiple language ecosystems (Rust Cargo, Go modules, mobile) requires coordinated workspace files: Cargo.toml for Rust crates and go.work for Go modules to maintain coherent build references. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Test-driven setup implementation allows early validation of …
  - effect: Go modules within a monorepo require individual go.mod/main.…
- Validate infrastructure setup (compose.yml, NATS config, VSS overlays) through integration tests to catch deployment configuration errors early in the CI pipeline. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Use regex with anchors (^target:) and QuoteMeta for robust M…
- Use regex with anchors (^target:) and QuoteMeta for robust Makefile target validation to avoid false matches on target names appearing in comments or variable definitions. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Validate infrastructure setup (compose.yml, NATS config, VSS…
- Use runtime.Caller(0) in a helper function to dynamically locate the repository root by walking up the directory tree until finding .git, making tests location-independent. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Test-only Go modules (like tests/setup) should have go.mod b…
  - effect: Use simple string matching (strings.Contains) rather than YA…
- When setting up test suites for a project structure specification, create test files that cover all subtasks upfront, even though most will fail initially until the actual project structure is implemented. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: In Go module test setups, some tests may pass immediately (l…
  - effect: Organizing tests into a separate `tests/` directory with sub…
- Use flat-Earth scaling factors of 111,320 metres per degree for latitude, and 111,320 × cos(midLat) for longitude, computed at the segment midpoint for better accuracy. _(spec: 08_parking_operator_adaptor, confidence: 0.60)_
  - cause: Distance-to-segment calculations for geographic data should …
  - effect: Distance-to-segment calculations for geographic data should …
- Use `extract_command_id()` best-effort JSON parsing (with `.filter()` to reject empty strings) to send error responses even when full command parsing fails. _(spec: 07_update_service, confidence: 0.60)_
- When using futures::future::pending() to block indefinitely on non-Unix platforms, nest it in platform-specific cfg blocks to avoid unused warnings. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: On Unix systems, use tokio::signal::unix::signal(SignalKind:…
- Idempotent writes (e.g., setting lock state) should check the current in-memory state before calling the broker to avoid redundant RPC calls and log the idempotent skip. _(spec: 02_data_broker, confidence: 0.60)_
  - effect: For best-effort JSON field extraction (e.g., extracting comm…
- For best-effort JSON field extraction (e.g., extracting command_id for error responses), use serde_json::from_str with .ok() and chain .and_then() to safely handle parse failures. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: Idempotent writes (e.g., setting lock state) should check th…
  - effect: Datapoint proto messages require setting timestamp field exp…
- Exponential backoff retry logic for gRPC connection attempts should store the last error and use an array of delays indexed by attempt number, returning after all attempts are exhausted. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: When implementing subscription streaming in tonic, spawn a b…
  - effect: Use tokio::select! with biased; to prioritize certain branch…
  - replaces: Implement exponential backoff retry logic with hardcoded delay array [1, 2, 4, 8…
- Extract command_id from potentially malformed JSON using best-effort parsing (serde_json::from_str on full value) to send error responses even when the command structure is invalid. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - effect: When JSON is valid but fields are missing/invalid, publish a…
- NATS client modules benefit from organizing publish operations by message type (commands, registration, telemetry) as separate methods for clarity and maintainability. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - cause: Cargo.toml should include both async-nats and tokio with tra…
- Table-driven tests with pre-verified test cases are preferred over algorithmic reference implementations (e.g., for point-in-polygon), as references like barycentric coordinates only work for specific polygon types. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - effect: Property-based tests should verify invariants across multipl…
- Stub implementations should be created across all sub-packages (model, config, geo, store, handler) before writing comprehensive test suites to ensure systematic coverage. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - effect: When creating a new service module with intentionally failin…
- Task group 2 of spec 03_locking_service includes config, command, safety, and response modules as distinct implementation units. _(spec: 03_locking_service, confidence: 0.60)_
  - effect: Unit test progression and clippy validation serve as key qua…
- Project setup verification should include comprehensive test coverage that validates all 18 setup aspects, with results documented in an errata document. _(spec: 01_project_setup, confidence: 0.60)_
  - cause: VSS signal customization is done via a vss-overlay.json file…
  - cause: Root Makefile should include targets for proto compilation, …
- Separating configuration loading (from_env) from data model definitions makes it easier to test and maintain environment-dependent logic independently. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - effect: Environment variable configuration should distinguish betwee…
- Property-based tests for safety-critical behavior should use proptest with property invariants (e.g., Property 2: speed priority) and run separately via --ignored flag. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - effect: Mock test objects using RefCell for interior mutability may …
- Use property-based testing (proptest) in addition to unit tests for locking service implementations to catch edge cases in concurrent/synchronization logic. _(spec: 03_locking_service, confidence: 0.60)_
  - cause: When implementing a Rust crate with comprehensive testing, o…
- Structure multi-language test suites (Rust + Go) with separate crate/module stubs that each have their own failing tests, allowing clean compilation while deferring implementation to GREEN phase. _(spec: 09_mock_apps, confidence: 0.60)_
  - cause: In a RED-phase test implementation, use exit code 0 for stub…
  - cause: Use HTTP 501 Not Implemented status in stub handlers during …
- Use HTTP 501 Not Implemented status in stub handlers during RED phase to satisfy test compilation while failing at runtime with expected behavior. _(spec: 09_mock_apps, confidence: 0.60)_
  - effect: Structure multi-language test suites (Rust + Go) with separa…
- Use simple string matching (strings.Contains) rather than YAML/JSON parsing to validate configuration files in setup tests, keeping tests lightweight and avoiding parsing dependencies. _(spec: 01_project_setup, confidence: 0.60)_
  - cause: Use runtime.Caller(0) in a helper function to dynamically lo…
  - cause: Test helper functions should be marked with t.Helper() to ex…
  - effect: Implement custom substring search helpers (containsStr, find…

## Decisions

- When a required field is missing from JSON, the service should only publish a response if command_id can be extracted; otherwise discard silently. This conditional response behavior (REQ-2.E2) narrows the broader requirement for unconditional responses (REQ-2.3), resolving the apparent contradiction between the two specifications. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - replaces: When a required field is missing from JSON, respond only if command_id can be ex…
- Unlock commands bypass all safety checks (speed and door state) and always succeed, while only lock commands are subject to safety constraints (REQ-3.4). _(spec: 05_parking_fee_service, confidence: 0.90)_
  - replaces: Unlock commands bypass all safety checks (speed, door state) and always succeed;…
- All Go modules must contain at least one test function (func Test*) in *_test.go files to satisfy compilation verification requirements and specification 01-REQ-8.2 (TS-01-27), even if only placeholder tests. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - replaces: All Go modules must contain at least one test function (func Test*) in *_test.go…
- Unset broker signals (None) are treated as safe defaults: missing speed → 0.0, missing door state → closed/false (REQ-3.E1, REQ-3.E2). This applies across cloud gateway client and parking fee service specifications. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - replaces: Unset broker signals (None) are treated as safe defaults: speed=0.0 and door_ope…
- All Rust crates must contain at least one #[test] annotation in their source files to satisfy compilation verification requirements (01-REQ-8.1, TS-01-26), even if tests only contain placeholder assertions like assert(true). _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - replaces: All Rust crates must contain at least one #[test] annotation in their source fil…
- Speed check takes priority over door state in safety validation; if vehicle speed >= 1.0 km/h, return VehicleMoving immediately without further evaluation of door state (Property 2 priority). _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - replaces: Speed is evaluated before door state in safety checks; if speed >= 1.0 km/h, ret…
- On response publish failure, errors are logged and processing continues; the service must not crash or block further commands or execution (REQ-5.E1). _(spec: 05_parking_fee_service, confidence: 0.90)_
  - replaces: On response publish failure, log and continue processing; the service must not c…
- Zone matching uses an OR condition: a point matches if it's either inside the polygon OR within threshold distance of the polygon edge; both conditions should be checked independently. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: The ray-casting algorithm for PointInPolygon uses longitude …
  - cause: Distance-to-segment calculations for geographic data should …
  - replaces: Zone matching logic should first check polygon containment, then fall back to pr…
- When JSON is valid but fields are missing/invalid, publish a failure response if command_id is extractable; if not extractable, only log a warning without publishing. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Extract command_id from potentially malformed JSON using bes…
- The Makefile GO_PASSING_MODULES list must be updated to exclude modules with failing tests from specific task groups, using comments to document the reason (e.g., referring to errata docs). _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Stub implementations should clearly document that they are s…
- Only 'driver' door is supported; any other door name (passenger, rear_left, etc.) must be rejected with UnsupportedDoor error and reason='unsupported_door'. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Success responses must NOT include a reason field; failure r…
- Go workspace module ordering in go.work files should list modules alphabetically (e.g., cloud-gateway before parking-fee-service) to maintain consistent project structure. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - replaces: Go module ordering in go.work files should be alphabetical (cloud-gateway before…
- Store mutable state (e.g., `lock_state`) in the main loop and pass it by mutable reference to async functions rather than sharing via Arc<Mutex<_>>, for simpler single-threaded ownership. _(spec: 07_update_service, confidence: 0.60)_
  - cause: In async Rust services, use `#[tokio::main]` attribute macro…
- Document specification inconsistencies (like REQ-2.2 backoff count) and gaps in test coverage (REQ-7.E1, REQ-3.E1) via errata rather than blocking implementation. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - effect: Squashed commit of the following:

commit 770a905fe8812824e2…
- Pre-existing test conflicts between skeleton setup (spec 01) and sensor integration (spec 09) should be documented in errata files rather than resolved immediately. _(spec: 01_project_setup, confidence: 0.60)_

## Conventions

- Earth's mean radius is 6,371,000 metres and should be defined as a package constant for use in Haversine distance calculations. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: Distance-to-segment calculations for geographic data should …
- Environment variables (e.g., CONFIG_PATH) can be used to configure application behavior at startup, enabling flexible deployment without code changes. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: feat: implement mock PARKING_OPERATOR serve subcommand (task…
  - effect: feat: implement main package for cloud-gateway (task group 5…
- In async Rust services, use `#[tokio::main]` attribute macro to set up the async runtime, and initialize tracing before calling async business logic. _(spec: 07_update_service, confidence: 0.90)_
  - effect: Store mutable state (e.g., `lock_state`) in the main loop an…
  - effect: When subscribing to streaming gRPC responses, spawn a backgr…
- tonic-build must be added as a build-dependency (not regular dependency) in Cargo.toml to generate protobuf code at compile time without including it in the binary. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Use tonic::include_proto!() macro to include generated proto…
- DefaultConfig should provide sensible demo values (e.g., Munich data, port 8080, 500m threshold) to enable the application to run without external configuration. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: LoadConfig should read JSON configuration, fall back to Defa…
- Cargo.toml should include both async-nats and tokio with tracing/tracing-subscriber for a complete async Rust NATS client implementation. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: NATS client modules benefit from organizing publish operatio…
  - effect: Async-nats operations should be instrumented with tracing at…
- Helper functions in test code should be marked with t.Helper() to exclude them from test call stacks when reporting failures, improving error message clarity. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Property-based tests should verify invariants across multipl…
  - replaces: Test helper functions should be marked with t.Helper() to exclude them from test…
- All HTTP endpoints must set Content-Type: application/json header, including error responses, to maintain consistency across the API. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: HTTP handlers should return JSON error responses in a consis…
- Skeleton version string tests (TestRustSkeletonVersionStrings, TestGoSkeletonVersionStrings) should be included in the setup verification test suite to ensure language-specific skeleton compatibility. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: A complete build/test/lint verification cycle should include…
- When creating a new service module with intentionally failing tests during development, update the Makefile to exclude it from passing test suites (GO_PASSING_MODULES) to avoid CI/CD failures. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Stub implementations should be created across all sub-packag…
  - effect: Errata documentation should be updated alongside Makefile ch…
- Command payload validation should enforce command_id as non-empty string, action as one of specific enum values ('lock'/'unlock'), and doors as any JSON array. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Bearer token validation should check for exact 'Bearer <toke…
  - cause: Parse raw bytes as serde_json::Value before field-level vali…
- NATS server listens on port 4222 and Kuksa Databroker on port 55556 in Docker Compose deployments. _(spec: 01_project_setup, confidence: 0.90)_
- Use clippy with -D warnings flag to enforce strict linting during development, catching potential issues before code review. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Environment variable configuration should distinguish betwee…
- Success responses must NOT include a reason field; failure responses must include command_id, status, reason, and timestamp (03-REQ-5.3). _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Empty command_id string passes JSON deserialization but must…
  - cause: Only 'driver' door is supported; any other door name (passen…
  - effect: On response publish failure, log and continue processing; th…
- Unset safety signals (speed, door state) have safe defaults: unset speed treated as 0.0, unset door treated as closed (03-REQ-3.E1, 03-REQ-3.E2). _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Unlock commands bypass all safety checks (speed, door state)…
- Success responses must omit the reason field; failure responses must include it (REQ-5.3 and skip_serializing_if). _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: When a required field is missing from JSON, respond only if …
  - cause: Lock and unlock operations are idempotent: if the door is al…
  - replaces: Success responses must omit the reason field entirely (using serde skip_serializ…
- Document specification contradictions (e.g., REQ-2.3 vs REQ-2.E2) formally in an errata rather than resolving them silently, ensuring stakeholders are aware of deviations from original requirements. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: When implementing a Rust crate with comprehensive testing, o…
- Run Clippy with -D warnings during test-first implementation to catch linting issues early, even before functionality is complete. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Squashed commit of the following:

commit f6e78541f2091c45b3…
  - effect: Squashed commit of the following:

commit 770a905fe8812824e2…
- Organize Rust module structure by domain concern (errors.rs, models.rs, config.rs, validators, telemetry) rather than by layer, improving cohesion within each module. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Squashed commit of the following:

commit 770a905fe8812824e2…
  - effect: Squashed commit of the following:

commit 1218999e59bea70774…
- All Rust crates and Go modules must contain at least one placeholder test (using #[test] or func Test) even if only for compilation verification. _(spec: 09_mock_apps, confidence: 0.90)_
- Proto files should be validated with protoc compiler (e.g., protoc 34.1) to catch syntax errors before integration. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: When defining multiple related gRPC services (VAL, UpdateSer…
- Proto3 files should include package declarations and go_package options for proper code generation and organization across multiple service definitions. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: When defining multiple related gRPC services (VAL, UpdateSer…
- Skeleton binaries should print their name and version (v0.1.0) to stdout when invoked with no arguments and exit with code 0, as specified in 01-REQ-4.1 through 01-REQ-4.4. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Spec 01 skeleton behavior (exit 0 with no args) is fundament…
- Go app main functions should print version information and exit with code 0 as standard setup behavior. _(spec: 01_project_setup, confidence: 0.90)_
- Rust skeleton binaries should include flag-rejection and version-string printing functionality as part of basic setup. _(spec: 01_project_setup, confidence: 0.90)_
- Go modules within a monorepo require individual go.mod/main.go files per module and a go.work file at the workspace root to coordinate multiple modules. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: A monorepo with multiple language ecosystems (Rust Cargo, Go…
- Reference specification requirements (TS-01-*, 01-REQ-*) directly in test comments to maintain traceability between test cases and specification requirements. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Test helper functions should be marked with t.Helper() to ex…
- Test-only Go modules (like tests/setup) should have go.mod but not main.go, and still need to be referenced in go.work for workspace consistency. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Use runtime.Caller(0) in a helper function to dynamically lo…
- In async Rust, use tracing_subscriber::fmt().with_env_filter() initialized with a default directive to provide structured logging; initialize before spawning async tasks. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: Use tokio::select! with biased; to prioritize certain branch…
  - effect: On Unix systems, use tokio::signal::unix::signal(SignalKind:…
- Initialize distributed tracing early in async main before spawning service tasks to capture the full request lifecycle. _(spec: 03_locking_service, confidence: 0.60)_
  - effect: Implement graceful shutdown by handling SIGTERM/SIGINT signa…
- Errata documentation should be updated alongside Makefile changes when excluding modules from test suites to maintain accurate project documentation. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - cause: When creating a new service module with intentionally failin…
- Unit test progression and clippy validation serve as key quality gates during incremental implementation of task groups. _(spec: 03_locking_service, confidence: 0.60)_
  - cause: Task group 2 of spec 03_locking_service includes config, com…
- Missing optional metadata fields (source, vin, timestamp) do not affect processing logic; they are preserved but ignored in command routing (03-REQ-2.4). _(spec: 03_locking_service, confidence: 0.60)_
- Property-based tests (proptest) verify invariants across arbitrary inputs; run separately via `cargo test -- --ignored` to avoid slowing down unit test suite. _(spec: 03_locking_service, confidence: 0.60)_
- All failure reasons (vehicle_moving, door_open, unsupported_door, invalid_command) are distinct and must be tested independently. _(spec: 05_parking_fee_service, confidence: 0.60)_
- Binary targets in Rust crates (e.g., 3 binaries in mock-sensors) are organized under src/bin/ rather than separate crates, enabling code sharing while supporting multiple entry points. _(spec: 01_project_setup, confidence: 0.60)_
  - cause: Test-driven setup implementation allows early validation of …
- Organizing tests into a separate `tests/` directory with subdirectories by feature (e.g., `tests/setup/`) helps structure test suites for different specification task groups. _(spec: 01_project_setup, confidence: 0.60)_
  - cause: When setting up test suites for a project structure specific…

## Anti-Patterns

- Implement custom substring search helpers (containsStr, findSubstr) instead of relying on strings package when building reusable test utilities, providing full control over edge cases. _(spec: 01_project_setup, confidence: 0.60)_
  - cause: Use simple string matching (strings.Contains) rather than YA…

## Fragile Areas

- Degenerate cases (empty polygon, single-vertex polygon, zero-length segment) must be handled explicitly; return MaxFloat64 for empty polygons and fall back to endpoint distance for degenerate segments. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: Distance-to-segment calculations for geographic data should …
- MockBrokerClient uses RefCell and is not Send/Sync; it only works under single-threaded tokio executors, not regular async tests. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Property-based tests (proptest) must run on a custom single-…
- The service treats invalid JSON (syntax errors) differently from missing required fields: InvalidJson errors result in silent discard, InvalidCommand errors trigger conditional response logic. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: When a required field is missing from JSON, the service shou…
