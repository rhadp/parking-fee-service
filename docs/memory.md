# Agent-Fox Memory

_603 facts | last updated: 2026-04-17_

## Gotchas

- When implementing a mock gRPC server with tonic, the Subscribe handler must call stream.SendHeader() before blocking operations, otherwise the client's .await will hang indefinitely. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: A self-contained integration test module can effectively val…
- The mock kuksa.VALService and real kuksa-databroker v0.5.0 use incompatible protobuf definitions (kuksa.VALService vs kuksa.val.v2.VAL), requiring conditional test skips and documented errata. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Integration tests should conditionally skip when external se…
- Go module names must match their import paths in tests; mismatches cause import failures. Use consistent naming like github.com/sdv-demo/mock/* for both module definition and test imports. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: feat: add integration tests for parking-operator-adaptor (ta…
  - effect: feat: add integration test module for mock apps (task group …
- For streaming gRPC tests, use grpcurl's -max-time flag to prevent the test from blocking indefinitely on open streams. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Implement port readiness polling with net.DialTimeout using …
- VSS overlay JSON files for kuksa-databroker must use properly nested tree format with `children` keys for branch nodes, not flat dot-notation, or parsing fails with 'children required for type branch' error. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Errata documents should map affected spec requirements and i…
- Eclipse Kuksa Databroker 0.5.0 exposes only `kuksa.val.v2.VAL` API, not v1; custom proto files must match the actual server implementation to avoid 'Unimplemented' RPC errors. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Proto field numbers must match the actual server wire format…
  - effect: Proto3 silently ignores unknown fields on both send and rece…
- Subscribe to NATS topics before spawning a service that publishes fire-and-forget messages, otherwise the subscription may miss the message. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: When testing service startup with file logging, redirect bot…
- In `kuksa.val.v2`, actuator signals require a registered provider via `OpenProviderStream`; use `PublishValue` instead of `Actuate` when acting as a signal source without a provider. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Proto file field numbers must match the actual server wire f…
- VSS overlay JSON files must use proper nested tree format with `children` keys for branch nodes, not flat dot-notation like `"Vehicle.Parking": {...}`. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Eclipse Kuksa Databroker 0.5.0 exposes only `kuksa.val.v2.VA…
- Eclipse Kuksa Databroker 0.5.0 exposes only `kuksa.val.v2.VAL` API, not the v1 API; custom proto files must be updated to match the actual server implementation. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: VSS overlay JSON files must use proper nested tree format wi…
  - effect: Proto file field numbers must match the actual server wire f…
- VSS (Vehicle Signal Specification) overlay files must use proper nested JSON tree format, not flat dot-notation. Kuksa expects hierarchical VSS definitions for correct signal resolution. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: fix: add description fields to branch nodes in VSS overlay (…
- When writing commands to Kuksa Databroker, use PublishValue instead of Actuate to avoid provider-not-registered errors. Actuate requires a registered provider which may not be available in all configurations. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: feat: validate DATA_BROKER integration tests against live da…
- The Kuksa Databroker 0.5.0 API uses kuksa.val.v2 proto specification, not a custom kuksa.VALService proto. Using the correct proto version is essential for compatibility with the real broker. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: feat: add integration tests and wire real gRPC broker (task …
  - effect: feat: validate DATA_BROKER integration tests against live da…
- Unknown command-line flags must be explicitly rejected using a starts_with('-') guard check to satisfy requirement 01-REQ-4.E1. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: add serve subcommand to Rust binaries for skeleton com…
- Container wait operations (podman wait) can fail independently from container exit codes; both failure modes should transition to ERROR state rather than assuming wait success means container health. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Container lifecycle management requires explicit state trans…
- When subscribing to NATS messages in tests, call Flush() after subscription setup to ensure the subscription is registered at the server before publishing, preventing race conditions where messages are missed. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Integration tests that capture subprocess output or rely on …
- Process output capture windows for startup tests must account for CPU starvation under parallel test execution; 500ms was insufficient, 2s provides safe headroom before NATS connection timeouts (~7s+). _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: fix: eliminate flaky TestStartupLogging in parking-fee-servi…
- Intermittent timing issues in tests can be resolved by increasing the capture/wait window (e.g., from 500ms to 2s) to account for system variability. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: fix: eliminate flaky TestStartupLogging in parking-fee-servi…
  - effect: Use Go build tags (e.g., '// +build integration') to separat…
- Race conditions in config tests can occur when using environment variables without proper synchronization; use mutexes (e.g., ENV_LOCK) to protect shared state in concurrent test scenarios. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Requirements traceability should map all requirement identif…
- kuksa-databroker requires intermediate branch nodes to be explicitly defined in flat VSS JSON format, even if only leaf nodes are used; omitting parent branches (e.g., Vehicle.Parking, Vehicle.Command) causes validation failures. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: VSS overlay validation can be tested statically without a li…
  - effect: Custom VSS signals must explicitly declare correct types (bo…
- Close the write end of an os.Pipe() in the parent process immediately after cmd.Start() so that the read end receives EOF when the subprocess exits, allowing io.Copy to detect process termination. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Use os.Pipe() for subprocess output capture in tests to avoi…
  - effect: Use os.Pipe() for subprocess output capture in tests to avoi…
- kuksa-databroker image tag :0.5.1 does not exist; use :0.5.0 instead when pinning versions. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: validate DATA_BROKER integration tests against live da…
- Use `#[serde(rename = "type")]` to map JSON field names that conflict with Rust keywords (e.g., `rate_type` field deserialized from `"type"` in JSON). _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: feat: implement model, config, and auth modules for cloud-ga…
- The kuksa-databroker:0.5.0 bundles VSS 4.0 (not VSS 5.1 as claimed in spec); functional requirements are met as all required signals exist in VSS 4.0. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: The Eclipse Kuksa Databroker image tag :0.5.1 specified in t…
  - effect: Errata/specification mismatch documentation should clearly s…
- The kuksa.val.v2 gRPC service does not have a GetMetadata RPC; use ListMetadata instead and prepend signal paths to the response. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Errata/specification mismatch documentation should clearly s…
- The kuksa-databroker --address flag requires separate --port flag; combined format like '--address 0.0.0.0:55555' is invalid. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Errata/specification mismatch documentation should clearly s…
- The kuksa-databroker binary uses --unix-socket flag instead of the spec-mandated --uds-path flag for Unix Domain Socket configuration. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: The Eclipse Kuksa Databroker image tag :0.5.1 specified in t…
  - effect: Errata/specification mismatch documentation should clearly s…
- The Eclipse Kuksa Databroker image tag :0.5.1 specified in the spec does not exist in the registry; use :0.5.0 instead. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: The kuksa-databroker binary uses --unix-socket flag instead …
  - effect: The kuksa-databroker:0.5.0 bundles VSS 4.0 (not VSS 5.1 as c…
- Go workspace configuration (go.work) must be updated when adding new modules to ensure proper dependency resolution across the project. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: add integration test module for mock apps (task group …
  - cause: feat: add integration tests for update-service (task group 6…
- Failed NATS connection after all retry attempts should return NatsError::RetriesExhausted, which the caller should treat as a fatal error requiring exit with code 1. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: Exponential backoff retry for NATS connection uses the formu…
- After MAX_ATTEMPTS (5 total: 1 initial + 4 retries) connection failures, return NatsError::RetriesExhausted which should be treated as a fatal error requiring exit with code 1. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Exponential backoff retry for NATS connection uses formula: …
- Timeout cancellation race conditions can occur when a timer callback fires after a response is already set; mitigate by validating state (checking for existing response) inside the mutex-protected section before updating. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Use sync.Mutex to protect shared state in concurrent code, a…
  - effect: Run tests with the -race flag to detect and verify fixes for…
- Protobuf's `oneof` field generates a nested `enum` type (e.g., `datapoint::Value`) that must be pattern-matched to extract the actual value; use `and_then()` chains to safely unwrap nested `Option` types. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: When a gRPC service returns field-level errors (separate fro…
- When using tonic-generated protobuf code, suppress the `enum_variant_names` lint for the generated types module since protobuf naming conventions (e.g., `StringValue`, `BoolValue`) conflict with Rust clippy expectations but cannot be changed in generated code. _(spec: 02_data_broker, confidence: 0.90)_
- When Go test wildcards ('...') cause failures in stub sub-packages, scope the test target to root packages only for affected services rather than recursively testing all sub-packages. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Stub tests added in sub-packages by downstream task groups c…
- The standard library `http.Error()` function writes both a status code and a plain text body; for JSON error responses, manually set the Content-Type header and write JSON to ensure proper formatting. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Middleware in Go should follow the `func(http.Handler) http.…
- Use `#[allow(async_fn_in_trait)]` when defining async trait methods in Rust <1.75, and document that the trait is for internal use only without requiring `Send` bounds. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Suppress the `dead_code` lint during test-driven development…
- Use `#[allow(async_fn_in_trait)]` when defining async trait methods in Rust, as native async-in-trait is only supported on Rust ≥ 1.75 and the lint may be suppressed for internal-only traits without external consumers. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Use `tokio::test(flavor = "current_thread")` instead of defa…
- Speed boundary is exactly 1.0 km/h: speeds < 1.0 are safe, speeds ≥ 1.0 are considered moving. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Safety checks should prioritize speed over other constraints…
- Spec 01 skeleton requirements (exit 0 with version on no args) can conflict with later spec requirements (Spec 09 RED-phase tests expecting exit 1 on no args); conflicts should be documented in errata rather than treated as regressions. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: feat: add serve subcommand to Rust binaries for skeleton com…
- Spec 01 skeleton binaries print version and exit 0 with no arguments, which conflicts with spec 09 tests that expect exit code 1; these conflicts are pre-existing spec 09 RED-phase failures and not regressions. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Skeleton binaries must print a version string containing the…
- When using the `gh` CLI to fetch GitHub issues or PRs, validate URLs strictly against the pattern `https://github.com/{owner}/{repo}/issues/{number}` and halt with a usage error if invalid. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Autonomous code fixes should post a transparent analysis com…
- Tonic gRPC clients require explicit connection retry logic and health checks when connecting to remote services like KUKSA data broker. _(spec: 08_parking_operator_adaptor, confidence: 0.60)_
  - cause: Proto codegen must be configured in build.rs to compile .pro…
- The tokio-stream dependency is needed when using tonic and implementing streaming RPC handlers with broadcast channels. _(spec: 07_update_service, confidence: 0.60)_
  - effect: When implementing gRPC services with tonic, use broadcast ev…
- Use a sleep delay (200ms) after publishing NATS responses to allow async subscription handlers time to process messages before asserting on state changes. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - cause: fix: eliminate flaky TestStartupLogging in parking-fee-servi…
- When sending signals to a subprocess in tests, use os.Interrupt (SIGINT) or syscall.SIGTERM depending on the signal handling behavior being tested; the service must explicitly handle these signals. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - effect: Use os.Pipe() for subprocess output capture in tests to avoi…
- Go 1.25.0 is used in this module; ensure all dependencies (like nats.go v1.51.0) are compatible with the Go version specified in go.mod to avoid unexpected build or runtime issues. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - effect: Use sync.Mutex to protect concurrent access to in-memory map…
- In Makefile validation tests, use anchored regex patterns ((?m)^target:) to match target definitions at line start, avoiding false positives from target references in comments. _(spec: 01_project_setup, confidence: 0.60)_
  - effect: Verify workspace configurations (go.work, Cargo.toml) refere…

## Patterns

- Proto file compatibility and vendoring (e.g., parking_adaptor.proto and kuksa.val.proto) should be explicitly verified as part of the wiring verification process. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: Startup logging should explicitly output all configuration v…
- Startup logging should explicitly output all configuration values plus version information and a ready message to aid debugging and monitoring. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: Comprehensive test coverage should include unit tests, prope…
  - effect: Proto file compatibility and vendoring (e.g., parking_adapto…
- Comprehensive test coverage should include unit tests, property tests, and integration/smoke tests together, with clippy validation using -D warnings for strict linting. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: Requirements traceability should be verified by mapping each…
  - effect: Startup logging should explicitly output all configuration v…
- A self-contained integration test module can effectively validate adaptor behavior by mocking both gRPC services (DATA_BROKER via kuksa.VALService) and HTTP servers (PARKING_OPERATOR) alongside smoke tests for end-to-end flows. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: When implementing a mock gRPC server with tonic, the Subscri…
- Mock gRPC servers in tests should be instantiated and run as real servers rather than mocked to properly validate gRPC service interactions and configurations. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: feat: add E2E happy-path scenario test and configure service…
- Proto-generated stubs should be vendored into pb/ subdirectories within each module to keep generated code organized and avoid import path conflicts. _(spec: 09_mock_apps, confidence: 0.90)_
- SIGTERM and SIGINT signal handlers should be integrated into the async runtime's main loop to ensure graceful shutdown of gRPC servers and cleanup of resources. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: Channel-based command dispatch is an effective pattern for r…
- Implement port readiness polling with net.DialTimeout using exponential backoff (short timeout per attempt, longer overall timeout) to wait for services to become reachable. _(spec: 07_update_service, confidence: 0.90)_
  - effect: Use t.Cleanup() to register teardown operations (process kil…
  - effect: For streaming gRPC tests, use grpcurl's -max-time flag to pr…
- Build environment variable maps from os.Environ() by splitting on '=' with SplitN(kv, '=', 2), then merge overrides before converting back to []string for exec.Cmd.Env. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Use runtime.Caller(0) to dynamically determine the current f…
- Use a goroutine-safe wrapper (safeBuffer with sync.Mutex) around strings.Builder to capture process output from concurrent goroutines without race conditions. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Use runtime.Caller(0) to dynamically determine the current f…
  - effect: Use t.Cleanup() to register teardown operations (process kil…
- Implement skip conditions as helper functions that call t.Skip() when prerequisites (like grpcurl or podman) are unavailable, allowing tests to gracefully skip rather than fail on missing tools. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Use runtime.Caller(0) to dynamically determine the current f…
  - effect: Use a separate Unix-specific build file (helpers_unix_test.g…
- Use runtime.Caller(0) to dynamically determine the current file's path, then navigate relative to it using filepath.Join with '..' to find the repository root without hardcoding paths. _(spec: 07_update_service, confidence: 0.90)_
  - effect: Implement skip conditions as helper functions that call t.Sk…
  - effect: Use a goroutine-safe wrapper (safeBuffer with sync.Mutex) ar…
- Pre-existing test failures from incomplete spec implementations (todo!() stubs) should be documented and tracked separately to avoid conflating them with current work quality gates. _(spec: 07_update_service, confidence: 0.90)_
  - cause: test: write failing spec tests for parking-operator-adaptor …
- Mock HTTP servers should implement proper error responses (404 for not found, 400 for bad requests) to enable realistic client-side testing. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: feat: implement mock PARKING_OPERATOR serve subcommand (task…
- In-memory stores with mutex protection are a practical approach for mock/stub servers that need session lifecycle management without external dependencies. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: feat: implement mock PARKING_OPERATOR serve subcommand (task…
- Use http.Server.Shutdown() for graceful shutdown handling of HTTP servers in response to SIGTERM/SIGINT signals. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: feat: implement mock PARKING_OPERATOR serve subcommand (task…
- When a service subscribes to multiple signals via gRPC, use a single shared multiplexed Channel and clone the client per call; this is cheap and avoids opening redundant connections. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Proto3 silently ignores unknown fields on both send and rece…
- Use `VAL/PublishValue` instead of `VAL/Actuate` when writing command signals without a registered actuator provider; Actuate requires a provider stream or returns 'Unavailable' status. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Errata documents should map affected spec requirements and i…
- Use a shared multiplexed `Channel` in tonic clients so that cloning the client per call is cheap and does not open redundant connections. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Create a helper function to construct option-wrapped proto v…
- Proto file field numbers must match the actual server wire format to ensure interoperability; generate protos from the real server's API definition rather than custom specifications. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Eclipse Kuksa Databroker 0.5.0 exposes only `kuksa.val.v2.VA…
  - cause: In `kuksa.val.v2`, actuator signals require a registered pro…
  - effect: Create a helper function to construct option-wrapped proto v…
- Graceful shutdown in Rust async servers should include a drain timeout (e.g., 10 seconds) to allow in-flight requests to complete before force termination. _(spec: 07_update_service, confidence: 0.90)_
  - cause: When implementing gRPC services with tonic, use broadcast ev…
  - effect: Signal handling for server shutdown should trap both SIGTERM…
- When implementing gRPC services with tonic, use broadcast event watching to wire container monitors into RPC handlers for real-time state updates. _(spec: 07_update_service, confidence: 0.90)_
  - cause: The tokio-stream dependency is needed when using tonic and i…
  - effect: Graceful shutdown in Rust async servers should include a dra…
- Clap is suitable for building command-line interfaces for sensor binaries that need to accept configuration parameters. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Passing all spec tests with zero clippy warnings indicates p…
- Tonic can be used to implement real gRPC client helpers for publishing datapoints, providing type-safe communication with gRPC services. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Passing all spec tests with zero clippy warnings indicates p…
- Proto files can be vendored directly into the project repository under a proto/ directory rather than fetched from external sources, simplifying build dependencies. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Passing all spec tests with zero clippy warnings indicates p…
- Live tests that depend on Podman should gracefully skip when Podman is unavailable, rather than fail. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: add E2E test infrastructure with Containerfiles and co…
- Validate numeric environment variables at parse time (e.g., GRPC_PORT as valid u16) rather than deferring validation to runtime. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: fix: eliminate flaky TestStartupLogging in parking-fee-servi…
- Use a mutex (ENV_LOCK) to protect environment variable access in parallel unit tests to prevent test pollution and race conditions. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: fix: eliminate flaky TestStartupLogging in parking-fee-servi…
- Integration tests in Rust can be marked with #[ignore] attribute to prevent them from running in standard test suites, allowing them to be run selectively or in specific CI/CD stages. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: When adding integration tests to an existing Rust project, v…
- Background periodic tasks (like container monitoring) should be spawned as separate timer threads and monitored independently, with explicit exit code handling (exit 0 = success, non-zero = error). _(spec: 07_update_service, confidence: 0.90)_
  - effect: Container lifecycle management requires explicit state trans…
- Container lifecycle management requires explicit state transitions (STOPPED → OFFLOADING → removed) with error handling that sets ERROR state on failure, ensuring containers are cleaned up via podman rm + rmi. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Background periodic tasks (like container monitoring) should…
  - effect: Container wait operations (podman wait) can fail independent…
- When testing message headers (e.g., NATS Authorization headers), verify them alongside payload content to ensure request context is properly propagated through the system. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: End-to-end smoke tests should verify the complete flow step-…
- Skip integration tests gracefully using t.Skipf() when checking for service availability (e.g., using nc -z) rather than failing, to allow test suites to run on environments without the service. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Integration tests should use build tags (//go:build integrat…
  - effect: Use helper functions like skipIfNATSUnavailable to centraliz…
- Integration tests should use build tags (//go:build integration) to allow optional execution only when external services (like NATS) are available. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Skip integration tests gracefully using t.Skipf() when check…
- Smoke tests verifying gRPC services should call basic methods like GetServerInfo to validate service health, and query metadata endpoints to verify expected signal inventories. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: add E2E happy-path scenario test and configure service…
- Smoke tests should manage container lifecycle conditionally and implement graceful skips when dependencies (Podman, containers) are unavailable rather than failing hard. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: refactor: decouple E2E container builds from compose

Build …
- Test-local build tags (e.g., //go:build integration) allow conditional test compilation; use them to separate integration tests from unit tests to avoid external dependencies in normal test runs. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: feat: add E2E test infrastructure with Containerfiles and co…
- End-to-end integration tests should use httptest.Server with the full handler stack and real NATS connections to validate the complete request flow from REST through message publishing to response handling. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: feat: add E2E happy-path scenario test and configure service…
- When testing NATS subscriptions, use rawConn.Flush() after subscribing to ensure the subscription is registered at the server before publishing, to avoid race conditions where messages are missed. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: feat: add integration tests for cloud-gateway (task group 6)…
- Integration tests that depend on external services (NATS) should use helper functions to skip gracefully with informative messages when the service is unavailable, rather than failing. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: feat: add integration tests for cloud-gateway (task group 6)…
- Use Go build tags (e.g., '// +build integration') to separate integration tests from unit tests, allowing selective test execution. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Intermittent timing issues in tests can be resolved by incre…
- Makefile maintenance should track implementation status by including fully-implemented modules and excluding those with incomplete stubs (todo! markers) to prevent premature inclusion in builds. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Requirements traceability should map all requirement identif…
- UpdateService install_adapter flow requires validation and enforces a single-adapter constraint before performing async pull/verify/run task sequence. _(spec: 07_update_service, confidence: 0.90)_
  - cause: StateManager requires separate methods for each operation (c…
  - effect: When organizing large task groups with multiple failing test…
- RealPodmanExecutor should use tokio::process::Command for executing all CLI operations asynchronously rather than spawning blocking processes. _(spec: 07_update_service, confidence: 0.90)_
  - effect: StateManager requires separate methods for each operation (c…
- A complete startup sequence for a cloud gateway client follows the order: config initialization → NATS connection → DATA_BROKER setup → service registration → spawn async processing loops. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Tracing-subscriber initialization should be configured durin…
  - effect: A cloud gateway client typically requires three concurrent a…
- Container image version pinning should be verified at runtime using 'podman ps' to ensure the correct tagged image is running, not just at compose configuration time. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: add E2E test infrastructure with Containerfiles and co…
- Podman compose commands should be run synchronously with a timeout (e.g., 20s) and assert non-zero exit codes when testing error cases, with graceful skip when Podman is unavailable. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: refactor: decouple E2E container builds from compose

Build …
- The load_config() function should handle missing files gracefully by falling back to defaults, while returning errors for invalid JSON. _(spec: 07_update_service, confidence: 0.90)_
- The derive_adapter_id() function extracts the last path segment and replaces colons with hyphens as part of adapter ID generation. _(spec: 07_update_service, confidence: 0.90)_
- Go integration tests can be designed to pass without infrastructure by using conditional skips when external services like Podman/DATA_BROKER are unavailable. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: feat: add E2E test infrastructure with Containerfiles and co…
- gRPC clients with tonic can implement exponential backoff for connection resilience in the connect() method. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Tonic requires build.rs with tonic-build to compile vendored…
- VSS overlay validation can be tested statically without a live container by checking structure, types, branch nodes, and descriptions in unit tests. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: kuksa-databroker requires intermediate branch nodes to be ex…
- Use conditional test skips (t.Skipf) when tests depend on external services (e.g., NATS server) that may not be available in all environments, rather than failing the test suite. _(spec: 05_parking_fee_service, confidence: 0.90)_
- When testing service startup without external dependencies, wait only until startup logs appear (before connection attempts) rather than waiting for full service readiness, to avoid test timeouts. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Log startup information (version, port, NATS URL, token coun…
  - effect: Use os.Pipe() for subprocess output capture in tests to avoi…
- In graceful shutdown handlers, drain the message broker (NATS) before shutting down the HTTP server to allow in-flight messages to complete before terminating connections. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Use os.Pipe() for subprocess output capture in tests to avoi…
- Log startup information (version, port, NATS URL, token count) immediately after config load but before connecting to external services, allowing verification of configuration without waiting for service availability. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Apply default values to configuration durations (e.g., comma…
  - effect: When testing service startup without external dependencies, …
- Use os.Pipe() for subprocess output capture in tests to avoid data races: the exec goroutine writes to the pipe while a separate goroutine reads from it, ensuring reads complete before calling cmd.Wait(). _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: In graceful shutdown handlers, drain the message broker (NAT…
  - cause: When testing service startup without external dependencies, …
  - effect: Close the write end of an os.Pipe() in the parent process im…
- Register auth middleware in HTTP route setup to ensure authentication is enforced across routes consistently. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Go 1.22 ServeMux supports pattern matching for HTTP routes n…
  - cause: Tests for startup logging should run before attempting exter…
- Graceful shutdown in Go servers should combine NATS drain() and http.Server.Shutdown() to cleanly close connections and allow in-flight requests to complete. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Use exponential backoff retry strategy (5 attempts) when con…
  - effect: Tests for graceful shutdown should handle unavailable extern…
- Use exponential backoff retry strategy (5 attempts) when connecting to external services like NATS to handle transient failures gracefully. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Graceful shutdown in Go servers should combine NATS drain() …
- Go 1.22 ServeMux supports pattern matching for HTTP routes natively, eliminating the need for external routing libraries in simple cases. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Register auth middleware in HTTP route setup to ensure authe…
- Dual listener configuration (TCP and UDS) in kuksa-databroker is supported via separate --address/--port and --unix-socket arguments in compose.yml. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: add E2E happy-path scenario test and configure service…
- To expose a UDS socket from a container to the host, bind a named volume to the /tmp directory on both container and host paths. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: add E2E test infrastructure with Containerfiles and co…
- Use `env!("CARGO_PKG_VERSION")` macro to embed package version at compile time rather than hardcoding version strings. _(spec: 07_update_service, confidence: 0.90)_
- Lock/unlock events should be idempotent: consecutive lock events (true) should result in only one start call, and consecutive unlock events (false) should result in only one stop call. _(spec: 07_update_service, confidence: 0.90)_
  - effect: Manual overrides (gRPC commands) should not persist across r…
- In-memory session state should be treated as authoritative; if external signal publish (e.g., DATA_BROKER) fails, log the error and continue rather than failing the operation. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Manual overrides (gRPC commands) should not persist across r…
- Use wiremock crate for HTTP mocking in tests; `up_to_n_times()` limits how many times a mock response is used, enabling sequential response testing. _(spec: 07_update_service, confidence: 0.90)_
- Serde JSON field renaming with `#[serde(rename = "...")]` allows parsing fields with names that conflict with Rust keywords or conventions. _(spec: 07_update_service, confidence: 0.90)_
- Use `tokio::test(flavor = "current_thread")` when testing with `RefCell`-based mocks to avoid Send+Sync requirement issues. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Mock objects in tests should not require `Send + Sync` bound…
- Mock objects in tests should not require `Send + Sync` bounds if they use `RefCell` for interior mutability in single-threaded contexts. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Use `#[allow(async_fn_in_trait)]` when defining async method…
  - effect: Use `tokio::test(flavor = "current_thread")` when testing wi…
- Use `todo!()` macros extensively in stubs to provide clear compilation targets and document what will be implemented in later task groups; include descriptive messages. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: test: write failing spec tests for parking-operator-adaptor …
- Implement idempotent lock/unlock event handlers so consecutive identical events (e.g., multiple lock events) do not trigger redundant operator calls; no-op if already in target state. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: feat: implement session module for parking-operator-adaptor …
- Design error handling to distinguish between operational failures (no panic) and state errors; log broker publish failures but continue processing so in-memory state remains authoritative. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: feat: implement operator client, broker, and gRPC handlers (…
- Stub `build.rs` with an empty `fn main() {}` to satisfy `build-dependencies` entries without implementation, deferring actual proto compilation to later task groups. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: test: write failing spec tests for parking-operator-adaptor …
- Use `RefCell` instead of `Mutex` in mock types for single-threaded tokio tests (with `current_thread` flavor) to avoid unnecessary synchronization overhead. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: test: write failing spec tests for parking-operator-adaptor …
- Use `#[allow(async_fn_in_trait)]` to permit async methods in trait definitions, enabling mock implementations in single-threaded tests without requiring `Send + Sync` bounds. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: test: write failing spec tests for parking-operator-adaptor …
- Property tests can be marked with #[ignore] to defer execution while building out the test suite, allowing the project to focus on unit tests first before running computationally expensive property-based tests. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: When setting up a test-driven development (TDD) project stru…
- When setting up a test-driven development (TDD) project structure, create stub implementations with todo!() macros and establish a complete test suite before implementation to ensure all tests initially fail. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: Property tests can be marked with #[ignore] to defer executi…
  - effect: Using clippy with -D warnings flag during initial project se…
- For Rust projects with protobuf code generation, build.rs must be configured alongside Cargo.toml dependency updates to ensure proto compilation happens during the build process. _(spec: 07_update_service, confidence: 0.90)_
- When implementing a failing spec test suite, organize source files with todo!() stubs and separate unit tests (expected to fail) from property tests (marked #[ignore]) to clearly distinguish test phases. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Property tests should be marked with #[ignore] during initia…
  - effect: Property tests should be marked with #[ignore] during initia…
- Graceful shutdown of message broker clients should use a Drain operation to allow in-flight messages to complete before closing connections. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Exponential backoff retry strategy for connection attempts s…
  - effect: Race condition testing with Go's race detector should be par…
- Handler implementations should validate incoming payloads before publishing to message brokers to fail fast and return appropriate HTTP error codes. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Authorization headers should be propagated from incoming req…
  - effect: HTTP handlers returning 202 Accepted for async operations sh…
  - effect: All HTTP responses should explicitly set Content-Type: appli…
- Authorization headers should be propagated from incoming requests through to downstream message broker calls (NATS publish) to maintain authentication context. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Handler implementations should validate incoming payloads be…
- HTTP handlers returning 202 Accepted for async operations should pair the response with a separate status lookup endpoint to allow clients to poll for completion. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Handler implementations should validate incoming payloads be…
- Exponential backoff retry strategy for connection attempts should use capped intervals (1s/2s/4s cap) with a maximum retry count limit to prevent unbounded connection attempts. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Graceful shutdown of message broker clients should use a Dra…
- Cross-transport gRPC tests (TCP vs UDS) should verify that both listeners are active simultaneously and that writes on one transport are visible to reads on the other. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: When testing gRPC APIs, use grpcurl with -plaintext flag and…
- Test code that modifies external files (e.g., overlay JSON) should use t.Cleanup() to restore original state regardless of test pass/fail. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Live integration tests should skip gracefully when external …
- Errata/specification mismatch documentation should clearly section each discrepancy with spec text, reality, and mitigation to guide implementation and test adaptation. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: The kuksa-databroker binary uses --unix-socket flag instead …
  - cause: The kuksa-databroker --address flag requires separate --port…
  - effect: Live integration tests should skip gracefully when external …
- Live integration tests should skip gracefully when external dependencies are unavailable; use helper functions to check connectivity (TCP port, UDS socket) before running test logic. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: Errata/specification mismatch documentation should clearly s…
  - cause: Test code that modifies external files (e.g., overlay JSON) …
  - effect: Errata/specification mismatch documentation should clearly s…
  - effect: Permissive mode testing should verify that requests with inv…
- When testing gRPC APIs, use grpcurl with -plaintext flag and specify the full service path including version (e.g., kuksa.val.v2.VAL/GetServerInfo). _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Cross-transport gRPC tests (TCP vs UDS) should verify that b…
  - effect: Test subscription streams with grpcurl by using --max-time f…
- Implement graceful shutdown by capturing SIGTERM and SIGINT signals and allowing in-flight requests to complete before exiting. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Test graceful shutdown behavior using subprocess tests to ve…
- Use slog for structured startup logging in Go services to provide clear initialization visibility and debugging information. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Load configuration from environment variables via CONFIG_PAT…
- Go 1.22 ServeMux supports direct route registration without third-party routers; use it for simple HTTP services with pattern-based routing. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: HTTP handlers should consistently set Content-Type: applicat…
- When implementing integration tests for containerized services, compose config tests should fail gracefully as expected during development while live gRPC tests skip when the container isn't running. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: add E2E happy-path scenario test and configure service…
  - cause: feat: add E2E test infrastructure with Containerfiles and co…
- The NatsClient struct holds both an async_nats::Client and the VIN string, allowing all methods to construct subject paths without requiring the VIN as a parameter. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: NATS subject paths are VIN-scoped and follow a consistent na…
- Command responses and telemetry are published as fire-and-forget (no acknowledgment beyond NATS confirm) with JSON payloads published verbatim without modification. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
- Exponential backoff retry for NATS connection uses the formula: delay_ms = BASE_BACKOFF_MS * (1u64 << (attempt - 1)), producing intervals of 1s, 2s, 4s, 8s for attempts 1-4, with a maximum of 5 total attempts. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: Failed NATS connection after all retry attempts should retur…
- Publish operations for responses and telemetry are fire-and-forget; they do not wait for acknowledgment beyond NATS publish confirmation. _(spec: 07_update_service, confidence: 0.90)_
- Exponential backoff retry for NATS connection uses formula: delay_ms = BASE_BACKOFF_MS * (1 << (attempt - 1)), producing intervals of 1s, 2s, 4s, 8s for attempts 1-4. _(spec: 07_update_service, confidence: 0.90)_
  - effect: After MAX_ATTEMPTS (5 total: 1 initial + 4 retries) connecti…
- Haversine formula is the standard approach for calculating great-circle distances between geographic coordinates. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Distance to polygon edge can be computed by combining flat-E…
- Ray casting algorithm can be used to implement PointInPolygon detection for geographical point-in-zone queries. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Distance to polygon edge can be computed by combining flat-E…
- Use sync.Mutex to protect shared state in concurrent code, and check for existing values inside critical sections to prevent race conditions where late-firing callbacks could overwrite valid data. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Timeout cancellation race conditions can occur when a timer …
- Implement idempotent lock/unlock by checking current state first; if already in desired state, return success without writing to the broker or running safety checks. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Safety checks (vehicle moving, door open) are required only …
- Use env!("CARGO_PKG_VERSION") macro to embed package version at compile-time for consistent versioning between binary output and metadata. _(spec: 01_project_setup, confidence: 0.90)_
- Use tracing_subscriber::EnvFilter::try_from_default_env() with a fallback default level to allow users to control log verbosity via RUST_LOG environment variable. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Implement re-subscription logic with exponential backoff whe…
- Implement re-subscription logic with exponential backoff when a streaming subscription fails or closes; track attempt count and exit after exhausting max retries. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Use tokio::select! with biased to prioritise shutdown signal…
  - effect: Use tracing_subscriber::EnvFilter::try_from_default_env() wi…
- Use tokio::select! with biased to prioritise shutdown signals over other operations; this ensures graceful shutdown can interrupt loops without waiting for the next event. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Check for field-level errors in gRPC responses (response.err…
  - effect: Implement re-subscription logic with exponential backoff whe…
- Check for field-level errors in gRPC responses (response.errors) and convert them to meaningful error messages instead of silently ignoring failures. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: When extracting values from protobuf oneof fields, pattern m…
  - cause: Distinguish between parse-time errors (non-JSON) and validat…
  - effect: Use tokio::select! with biased to prioritise shutdown signal…
- When extracting values from protobuf oneof fields, pattern match on the enum variant (e.g., kuksa::datapoint::Value::FloatValue) to safely unwrap and return None if the type doesn't match. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Use tokio::spawn to forward gRPC streaming responses to an m…
  - effect: Check for field-level errors in gRPC responses (response.err…
- Use tokio::spawn to forward gRPC streaming responses to an mpsc channel; check if the receiver is dropped (send returns Err) to stop streaming and avoid wasting resources. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Implement exponential backoff retry logic by incrementing de…
  - effect: When extracting values from protobuf oneof fields, pattern m…
- Implement exponential backoff retry logic by incrementing delays (1s, 2s, 4s, 8s) for connection failures; use bit shifting (1u64 << (attempt - 1)) to compute backoff duration. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Reuse a single shared tonic::transport::Channel across multi…
  - effect: Use tokio::spawn to forward gRPC streaming responses to an m…
- Reuse a single shared tonic::transport::Channel across multiple service calls by cloning the ValServiceClient per-call; this is cheap because the channel is internally multiplexed and does not open new connections. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Use tonic::include_proto! macro in a module to generate Rust…
  - effect: Implement exponential backoff retry logic by incrementing de…
- Use tonic::include_proto! macro in a module to generate Rust types from .proto files; suppress clippy::enum_variant_names lint since generated protobuf enums use naming conventions that conflict with Rust style. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: tonic-build must be added as a build-dependency in Cargo.tom…
  - effect: Reuse a single shared tonic::transport::Channel across multi…
- tonic-build must be added as a build-dependency in Cargo.toml to compile protobuf definitions via build.rs, separate from runtime dependencies. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Use tonic::include_proto! macro in a module to generate Rust…
- Use tracing instrumentation at INFO/WARN/ERROR levels across all client methods to provide observability for debugging and monitoring NATS operations. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Implement exponential backoff retry logic with specific inte…
  - cause: Organize NATS client functionality into distinct methods for…
- Implement exponential backoff retry logic with specific intervals (1s, 2s, 4s, 8s) for connection attempts in NATS client to handle transient failures gracefully. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Use tracing instrumentation at INFO/WARN/ERROR levels across…
- Auth middleware should skip authentication checks for specific health-check endpoints (e.g., /health) to allow monitoring without credentials. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Auth middleware should extract Bearer tokens from request he…
- Auth middleware should extract Bearer tokens from request headers, validate them against a config mapping, and verify that the token's VIN matches the VIN in the URL path before granting access. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: LoadConfig should return an error when the JSON file is miss…
  - effect: Auth middleware should skip authentication checks for specif…
  - effect: Auth middleware should return JSON-formatted 401 (Unauthoriz…
- LoadConfig should return an error when the JSON file is missing or contains invalid JSON, rather than silently failing or using defaults. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Auth middleware should extract Bearer tokens from request he…
- Implement idempotency by checking state before making mutations (e.g., return success without writing if already in desired state), reducing unnecessary broker calls and preventing spurious state transitions. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Design error types with specific reasons (e.g., `invalid_com…
- For JSON parsing when you may receive invalid JSON, use `serde_json::from_str::<serde_json::Value>()` to safely attempt parsing and extract fields via `get()` and `and_then()` chains, returning `None` on parse failure. _(spec: 02_data_broker, confidence: 0.90)_
- Use `tokio::select!` with `biased;` to prioritize certain branches (e.g., shutdown signals) over others, ensuring clean exit behavior even when multiple events are pending. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: For exponential backoff retry logic, use the formula `delay_…
- When a gRPC service returns field-level errors (separate from RPC-level errors), check the response object for an `errors` field and convert them to an application error rather than silently ignoring them. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: Protobuf's `oneof` field generates a nested `enum` type (e.g…
  - cause: Design error types with specific reasons (e.g., `invalid_com…
- For gRPC streams in tokio, spawn a task with `tokio::spawn()` to forward stream messages to an `mpsc::Receiver`, checking if the receiver is dropped (via `send().is_err()`) to gracefully stop the task when the channel closes. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: A shared `Channel` in tonic can be cloned cheaply and is int…
- Use `tonic::include_proto!()` macro in a private module to include generated protobuf types, then re-export only the needed types to keep the public API clean and contained. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: When building protobuf files with tonic, use `build.rs` with…
- When building protobuf files with tonic, use `build.rs` with `tonic_build::configure()` and set `build_server(false)` if only a client is needed, then call `compile()` with the proto file and include directory paths. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Use `tonic::include_proto!()` macro in a private module to i…
- For exponential backoff retry logic, use the formula `delay_ms = BASE_BACKOFF_MS * (1u64 << (attempt - 1))` to generate delays like 1s, 2s, 4s, 8s for attempts 1, 2, 3, 4. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Use `tokio::select!` with `biased;` to prioritize certain br…
- A shared `Channel` in tonic can be cloned cheaply and is internally multiplexed, so creating a new `ValServiceClient` per call using `ValServiceClient::new(channel.clone())` does not create new connections. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: For gRPC streams in tokio, spawn a task with `tokio::spawn()…
- Map-based indexing is an effective pattern for store implementations to enable efficient lookups by zone and operator identifiers. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Config loading should include fallback behavior for missing …
  - cause: Scaffolding task groups in advance (e.g., creating model pac…
- Config loading should include fallback behavior for missing files (e.g., loading demo defaults) and explicit error handling for invalid JSON to provide graceful degradation. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Map-based indexing is an effective pattern for store impleme…
- Graceful shutdown using SIGTERM/SIGINT handlers with async main loops requires proper signal handling and resource cleanup sequencing. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Exponential backoff retry logic is useful for gRPC connectio…
- Idempotent state updates in command processing prevent duplicate side effects when commands are retried or processed multiple times. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Extract command identifiers early in the request pipeline to…
- Exponential backoff retry logic is useful for gRPC connection establishment to handle transient network failures gracefully. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Graceful shutdown using SIGTERM/SIGINT handlers with async m…
- tonic-build requires vendored proto files and a build.rs script to generate gRPC client code from .proto definitions at compile time. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Extract command identifiers early in the request pipeline to…
- Bearer token validation should extract the token from the `Authorization: Bearer <token>` header and reject malformed headers (missing 'Bearer' prefix, double-Bearer, etc.) with HTTP 401. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Test error response format explicitly (e.g., `{"error":"mess…
- Test error response format explicitly (e.g., `{"error":"message"}`) to ensure consistency across all endpoints; decode and validate the 'error' key rather than assuming a specific structure. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Bearer token validation should extract the token from the `A…
- Use `*time.Timer` references in a map to track and cancel pending timeout operations; always lock the mutex before modifying or checking the timers map to prevent race conditions. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Use sync.Mutex to protect concurrent access to in-memory map…
- When testing timeout behavior, allow margin (e.g., sleep 2x the timeout duration) to account for scheduler variability; use `time.Since()` to measure elapsed time rather than exact sleep durations. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Property-based testing (verifying properties that must alway…
- Interface-based mocking (e.g., `NATSPublisher`) allows test code to verify interactions without a real external service; define interfaces for external dependencies and inject mock implementations in tests. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Property-based testing (verifying properties that must alway…
- Use sync.Mutex to protect concurrent access to in-memory maps (responses, timers) in Go; always lock before read and write operations to prevent data races, especially in multi-goroutine scenarios. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Go 1.25.0 is used in this module; ensure all dependencies (l…
  - effect: Use `*time.Timer` references in a map to track and cancel pe…
- Middleware in Go should follow the `func(http.Handler) http.Handler` pattern and return `http.HandlerFunc` wrapping the next handler to maintain composability and clean separation of concerns. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Go 1.22+ supports pattern-based HTTP routing syntax like `PO…
  - cause: Use unexported types (lowercase) for context keys in Go (e.g…
  - effect: The standard library `http.Error()` function writes both a s…
- Go 1.22+ supports pattern-based HTTP routing syntax like `POST /vehicles/{vin}/commands` directly in `http.ServeMux.HandleFunc()`, eliminating the need for third-party routers for basic REST APIs. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Middleware in Go should follow the `func(http.Handler) http.…
- Interface extraction (e.g., NATSPublisher interface) allows tests to mock external dependencies without requiring the real implementation. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Implement property-based tests (in addition to example tests…
- Bearer token validation should extract the token from the Authorization header and verify it exists in a configured token-to-VIN mapping. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Use unexported types (lowercase) for context keys to prevent…
  - effect: Always set Content-Type: application/json in REST JSON respo…
- Use httptest.NewRecorder() for synchronous HTTP handler testing; the Recorder captures all response data for assertion. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: HTTP middleware in Go should follow the pattern `func(http.H…
- Implement property-based tests (in addition to example tests) to verify invariants such as 'token-to-VIN isolation' or 'timeout cancellation behavior'. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Use sync.Mutex to protect concurrent access to in-memory map…
  - cause: Test helpers that modify test state (like makeTestConfig) sh…
- Use sync.Mutex to protect concurrent access to in-memory maps in Go; test thread safety with the `-race` flag. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Implement property-based tests (in addition to example tests…
- HTTP middleware in Go should follow the pattern `func(http.Handler) http.Handler` and return a closure that wraps the next handler. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Go 1.22+ supports named route parameters in http.ServeMux us…
  - effect: Use httptest.NewRecorder() for synchronous HTTP handler test…
- Go 1.22+ supports named route parameters in http.ServeMux using the syntax 'POST /vehicles/{vin}/commands', eliminating the need for external routing libraries. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: HTTP middleware in Go should follow the pattern `func(http.H…
- When implementing Go packages with stub files for TDD, ensure all packages compile successfully before writing test implementations to establish a valid baseline. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Running `go vet` with no warnings on stub implementations is…
- Validate required fields like command_id as non-empty strings and doors as present array during JSON payload validation before deserialization. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Use HeaderValue::as_str() to extract and validate Bearer tok…
  - cause: Validate enum command actions by checking membership in a fi…
- Validate enum command actions by checking membership in a fixed set (e.g., {lock, unlock}) before or during deserialization. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Validate required fields like command_id as non-empty string…
- When deserializing JSON payloads that may contain extra fields, use #[serde(flatten)] to preserve unknown fields rather than rejecting them. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
- Use HeaderValue::as_str() to extract and validate Bearer token strings from Authorization headers, checking for 'Bearer ' prefix and token presence. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Validate required fields like command_id as non-empty string…
- Organize Go service packages with modular structure (model, config, geo, store, handler) to separate concerns and enable independent testing of components. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Ensure go vet passes with no warnings during test scaffoldin…
- Use panic('not implemented') as a stub implementation pattern in Go to create compilable code that fails at runtime, enabling test-driven development workflows. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Ensure go vet passes with no warnings during test scaffoldin…
- When implementing a multi-task-group spec with stub functions (todo!()), unit tests for completed task groups should pass while integration tests depending on incomplete task groups (like process module) will fail as expected until those groups are implemented. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Deterministic unit tests are suitable for validating core bu…
- Proto code generation and build verification tests can be organized in a dedicated tests/setup/ directory with individual test functions for each toolchain (Rust, Go, Proto). _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Setup verification tests should invoke toolchains as subproc…
- Setup verification tests should invoke toolchains as subprocesses and skip gracefully when tools are absent, rather than failing hard. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Tasks may already exist from previous task groups; verify an…
  - effect: Proto code generation and build verification tests can be or…
- TelemetryState should omit never-set fields from JSON output to avoid sending null or uninitialized values (REQ-8.3). _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Prefix unused function parameters with underscore (e.g., _pa…
  - effect: RegistrationMessage should include current Unix timestamp an…
- Use ENV_MUTEX in config tests to prevent race conditions when tests run in parallel and modify shared environment variables. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Prefix unused function parameters with underscore (e.g., _pa…
- Use `#[serde(skip_serializing_if = "Option::is_none")]` on optional fields in response structs to omit them from JSON when not present, rather than serializing `null`. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Use `std::time::SystemTime::now()` and `UNIX_EPOCH` to gener…
- Implement idempotent state transitions: only write to broker signals if the requested state differs from the current state, avoiding unnecessary writes. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Check higher-priority safety constraints (e.g., speed) befor…
- Check higher-priority safety constraints (e.g., speed) before lower-priority ones (e.g., door state) and return early, as order matters for failure reason reporting. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Treat absent/unset signals from a broker as safe defaults (e…
  - effect: Implement idempotent state transitions: only write to broker…
- Treat absent/unset signals from a broker as safe defaults (e.g., speed → 0.0, door → closed) rather than as errors, to degrade gracefully. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Check higher-priority safety constraints (e.g., speed) befor…
- Use `RefCell` for recording mutable state in test doubles that need interior mutability but won't be shared across threads; document that they are not `Send`/`Sync`. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Use `#[tokio::test(flavor = "current_thread")]` for async te…
- Use `#[tokio::test(flavor = "current_thread")]` for async tests that use `RefCell` and interior mutability (non-Send), rather than the default multi-threaded flavor. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Use `RefCell` for recording mutable state in test doubles th…
- Use `#[allow(async_fn_in_trait)]` when defining async methods in traits on Rust >= 1.75, since native async-in-trait is supported and the lint can be suppressed for internal-only traits without external consumers. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Suppress `#![allow(dead_code)]` at crate level during TDD sk…
- Use `serde(skip_serializing_if = "Option::is_none")` to omit optional fields from JSON output when they are `None`, keeping responses minimal. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Rust serde `#[serde(rename_all = "lowercase")]` converts enu…
- For safety-critical operations, check constraints in priority order (speed before door state) and document this ordering in design specs and code comments. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Missing or unset signal values from a broker should default …
  - effect: Idempotent operations (lock/unlock) should check current sta…
- When a command response publish fails, log the error but continue processing subsequent commands; do not fail the entire service. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Idempotent operations (lock/unlock) should check current sta…
- Idempotent operations (lock/unlock) should check current state and skip writing to the broker if state already matches the requested action, reducing unnecessary I/O. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: For safety-critical operations, check constraints in priorit…
  - effect: When a command response publish fails, log the error but con…
- Missing or unset signal values from a broker should default to safe values (e.g., speed → 0.0, door → closed) rather than failing, to improve robustness. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: For safety-critical operations, check constraints in priorit…
- Use `RefCell` for interior mutability in test doubles when the mock is used in single-threaded async contexts (`#[tokio::test(flavor = "current_thread")]`); avoids `Send`/`Sync` requirements. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Property-based tests with proptest should be marked `#[ignor…
- Rust serde `#[serde(rename_all = "lowercase")]` converts enum variant names to lowercase during deserialization, enabling case-insensitive JSON parsing (e.g., "lock" → `Action::Lock`). _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Use `serde(skip_serializing_if = "Option::is_none")` to omit…
- For safety-critical code, document signal paths and safety constraints (e.g., speed threshold, door state) as module-level constants and error types to make assumptions explicit. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Use `#[serde(skip_serializing_if = "Option::is_none")]` on o…
  - cause: In Cargo.toml, explicitly define `[[bin]]` section with `nam…
- Implement mock clients using `RefCell<Vec<T>>` to record method calls without `&mut` receivers, enabling use in trait implementations where `&self` is required. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Use `tokio::test(flavor = "current_thread")` instead of defa…
  - effect: Use builder pattern (method chaining with `self`) for test d…
- Use `tokio::test(flavor = "current_thread")` instead of default flavor when testing with `RefCell`-based interior mutability mocks, as the default multi-threaded flavor requires `Send` and `Sync` bounds. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Use `#[allow(async_fn_in_trait)]` when defining async trait …
  - effect: Implement mock clients using `RefCell<Vec<T>>` to record met…
- In Rust tests that manipulate environment variables, use `std::env::remove_var()` after assertions to avoid polluting other tests, especially when tests run in parallel. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: For property-based tests using proptest, mark them with `#[i…
- Use `serde(skip_serializing_if = "Option::is_none")` to omit the `reason` field from success responses while including it in failure responses. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Continue processing after response publish failures; log the…
- Implement idempotent lock/unlock operations: only call `set_bool` if the desired state differs from the current state. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Treat missing sensor signals as safe defaults: absent speed …
  - effect: Continue processing after response publish failures; log the…
- Record mock calls using `RefCell<Vec<T>>` for interior mutability in test doubles, allowing immutable methods to record state without requiring `&mut self`. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Use `#[tokio::test(flavor = "current_thread")]` for tests th…
  - effect: Mark property-based tests with `#[ignore]` so they do not ru…
- Use `#[tokio::test(flavor = "current_thread")]` for tests that use `RefCell` or other non-`Send` types like `MockBrokerClient`, avoiding the need for `Send`/`Sync` bounds. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Use `#[allow(async_fn_in_trait)]` to suppress the lint when …
  - effect: Record mock calls using `RefCell<Vec<T>>` for interior mutab…
- Use `#[allow(async_fn_in_trait)]` to suppress the lint when defining async methods directly in traits, which is supported in Rust ≥ 1.75 without requiring `Send` bounds for internal-only traits. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Use `#[tokio::test(flavor = "current_thread")]` for tests th…
- Use MockBrokerClient and todo!() stubs to scaffold failing tests that compile cleanly while establishing the test contract before writing implementation logic. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: In TDD RED phase, create all failing spec tests first before…
  - effect: Use ignored property tests (via #[ignore]) to preserve test …
- In TDD RED phase, create all failing spec tests first before implementation, organizing them across multiple source files with clear module separation (broker, command, safety, response, etc.). _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Use MockBrokerClient and todo!() stubs to scaffold failing t…
- A test-driven development approach for Rust modules involves creating a full skeleton with todo!() stubs across all modules (lib.rs, errors.rs, models.rs, etc.) before implementing functionality, allowing all tests to compile and fail at runtime. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Integration tests that are not yet ready should be marked wi…
- Use `starts_with('-')` to detect unknown flag arguments in Rust skeleton binaries, and call `eprintln!` followed by `std::process::exit(1)` to reject them. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: Skeleton binaries should print a version string containing t…
- Pre-existing RED-phase (failing) tests from future specs should not be treated as regressions when earlier specs' implementations diverge; document discrepancies in errata files. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: All Rust crates must contain at least one unit test marked w…
  - cause: All Go modules must contain at least one test file (*_test.g…
- Skeleton binaries should print a version string containing the component name and exit 0 when invoked with no arguments, but reject any flag arguments (starting with '-') by printing usage to stderr and exiting with code 1. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Use `starts_with('-')` to detect unknown flag arguments in R…
  - effect: Skeleton comments should reference 'a future spec' rather th…
- Proto files should be validated with protoc during development to catch syntax errors early, especially when coordinating multiple service definitions. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Proto3 files should include package declarations and go_pack…
  - cause: When defining multiple related proto services (update, adapt…
- When defining multiple related proto services (update, adapter, gateway, databroker), organize them into separate files by service domain rather than combining into a single file for better modularity and maintainability. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Proto files should be validated with protoc during developme…
- Go skeleton modules should print a version string containing the component name to stdout when invoked without arguments. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: test: write spec tests for mock apps (task group 1)

- Add V…
- Rust skeleton binaries should reject any command-line arguments starting with '-' by printing usage to stderr and exiting with code 1, while accepting no arguments and printing a version string. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: feat: add serve subcommand to Rust binaries for skeleton com…
- Rust skeleton binaries should check for flag arguments (starting with '-'), print usage to stderr via eprintln!, and exit with code 1 when unknown flags are provided. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Go skeleton mains should be simplified to only import necess…
- Use subprocess-based integration tests for CLI applications to verify end-to-end behavior across multiple binaries (companion-app-cli, parking-app-cli) in a realistic execution environment. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: Cargo workspaces with multiple binaries (stub or otherwise) …
- Go modules in a monorepo can be coordinated using a go.work file that references multiple module directories, enabling unified dependency management. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: In a polyglot monorepo, deferring infrastructure and build s…
- A Rust Cargo workspace can be organized with multiple member crates, and individual crates can declare multiple binary targets (e.g., mock-sensors with 3 binaries). _(spec: 01_project_setup, confidence: 0.90)_
  - effect: In a polyglot monorepo, deferring infrastructure and build s…
- Structure setup validation tests to verify both configuration files (go.mod, Cargo.toml) and accompanying source files (main.go, src/main.rs) exist together. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Document test specifications (TS-01-*) and requirement mappi…
- Verify workspace configurations (go.work, Cargo.toml) reference all required modules/members to catch integration issues early. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: In Makefile validation tests, use anchored regex patterns ((…
- Use subtests (t.Run) to organize related assertions and provide granular test failure reporting, especially when validating multiple related conditions. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: In Go test files, use t.Helper() in helper functions to ensu…
  - effect: Document test specifications (TS-01-*) and requirement mappi…
- Use runtime.Caller(0) with filepath navigation to dynamically locate the repository root in tests, making tests resilient to execution directory changes. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: In Go test files, use t.Helper() in helper functions to ensu…
- Tests can be written to fail at runtime before the actual project structure exists, serving as specifications for implementation; using `go vet` to validate test compilation ensures test quality before implementation. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: When setting up test modules in Go, creating the module dire…
- When setting up test modules in Go, creating the module directory (go.mod) itself satisfies existence checks for that directory, allowing some tests to pass immediately while others fail due to missing implementation. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Tests can be written to fail at runtime before the actual pr…
- Before implementing a fix, establish a green baseline by running the project's full test suite to document the current state and understand pre-existing test failures. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Autonomous code fixes should post a transparent analysis com…
- Autonomous code fixes should post a transparent analysis comment to the GitHub issue before implementing changes, explaining the diagnosis, root cause, planned approach, and assumptions made. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: When using the `gh` CLI to fetch GitHub issues or PRs, valid…
  - cause: Before implementing a fix, establish a green baseline by run…
  - effect: Use conventional commit messages with format `{type}({scope}…
- When simplifying code, prioritize maintainability first, then readability, then reduced complexity—lines of code and file count are signals of good simplification, not the primary goal. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Code consolidation should merge files that are always modifi…
  - effect: Never refactor test code primarily for DRYness—tests should …
- When documenting architectural decisions, code is the single source of truth—verify all claims against actual implementation rather than relying on documentation, README, or comments when they diverge. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: ADRs must document at least: context (the problem), decision…
- Autonomous code fixes require pre-flight checks: verify working tree is clean, derive branch name from issue title with conventional prefix (`fix/` or `feature/`), and check for existing remote branches before proceeding. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: When implementing fixes autonomously, classify issues by typ…
  - effect: Bash permission settings in `.claude/settings.local.json` mu…
- When implementing fixes autonomously, classify issues by type (bug, feature, refactor, performance), perform root cause analysis rather than treating symptoms, and check for related issues that may share the same class of bug. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: The `af-fix` autonomous fixer uses a single-pass mandate: co…
  - effect: Autonomous code fixes require pre-flight checks: verify work…
- The `af-fix` autonomous fixer uses a single-pass mandate: complete all steps without halting for confirmation, only requesting clarification when an issue has genuinely contradictory interpretations that lead to fundamentally different fixes. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Bash permission settings in `.claude/settings.local.json` mu…
  - effect: When implementing fixes autonomously, classify issues by typ…
- Code simplification should eliminate over-engineering such as interfaces with only one implementation, wrapper classes that simply delegate without adding logic, and future-proofing abstractions serving no current use case. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: When simplifying code, maintainability always wins over redu…
- GitHub issue cloning via `gh` CLI requires fetching source issue JSON, checking that `pull_request` field is absent (to confirm it's an issue not a PR), and replaying paginated comments in chronological order on the target repo. _(spec: 01_project_setup, confidence: 0.90)_
- When creating ADRs, the code is the single source of truth; always verify claims against actual implementation rather than relying on documentation, which frequently diverges from what was actually implemented. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: ADRs should document at least two real alternatives with hon…
- End-to-end smoke tests should cover full lifecycle sequences (e.g., parking-operator lifecycle, lock-status sequences) to catch integration failures early. _(spec: 09_mock_apps, confidence: 0.60)_
  - cause: Integration tests should conditionally skip when external se…
  - cause: Property tests with timestamp/duration combinations (10+ cas…
- Property tests with timestamp/duration combinations (10+ cases) are useful for verifying session integrity across temporal edge cases. _(spec: 09_mock_apps, confidence: 0.60)_
  - effect: End-to-end smoke tests should cover full lifecycle sequences…
- Integration tests should conditionally skip when external service interfaces differ between mock and production implementations rather than failing outright. _(spec: 09_mock_apps, confidence: 0.60)_
  - cause: The mock kuksa.VALService and real kuksa-databroker v0.5.0 u…
  - effect: End-to-end smoke tests should cover full lifecycle sequences…
- Channel-based command dispatch is an effective pattern for routing gRPC service requests to event loop handlers in async Rust applications. _(spec: 08_parking_operator_adaptor, confidence: 0.60)_
  - cause: Proto codegen must be configured in build.rs to compile .pro…
  - effect: SIGTERM and SIGINT signal handlers should be integrated into…
- Skip tests on infrastructure issues (network problems, unavailable registries) by detecting specific error strings in grpcurl output rather than treating them as test failures. _(spec: 07_update_service, confidence: 0.60)_
  - cause: Use t.Cleanup() to register teardown operations (process kil…
- Integration tests that spawn service subprocesses should capture logs to a temp file and inspect them after process termination to diagnose startup failures without relying on stdout/stderr pipes. _(spec: 02_data_broker, confidence: 0.60)_
  - effect: Mark integration tests that require external containers (NAT…
- Proto3 silently ignores unknown fields on both send and receive sides; intentionally omit unused fields (e.g., timestamp) from generated message types to simplify deserialization. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: Eclipse Kuksa Databroker 0.5.0 exposes only `kuksa.val.v2.VA…
  - cause: When a service subscribes to multiple signals via gRPC, use …
- Implement retry logic with exponential backoff using inline loops rather than external crates, with a pattern of N total attempts (1 initial + N-1 retries) and exponential delay scaling (1s, 2s, 4s, etc.). _(spec: 08_parking_operator_adaptor, confidence: 0.60)_
  - effect: REST client implementations for external services should use…
- Use `env_remove()` in tests to explicitly remove environment variables (e.g., VIN) rather than relying on absence, to ensure the test environment does not inherit unintended values. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
- When testing service startup with file logging, redirect both stdout and stderr to the same file and kill the process after the observation window to ensure logs are flushed. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - cause: Exponential backoff tests should allow a generous time windo…
  - cause: Subscribe to NATS topics before spawning a service that publ…
- Exponential backoff tests should allow a generous time window (8–30 seconds for a 15-second backoff schedule) to account for system variance and CI slowness. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - effect: When testing service startup with file logging, redirect bot…
- Create a helper function to construct option-wrapped proto values (e.g., `make_value(TypedValue)`) to reduce boilerplate in unit tests. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - cause: Proto file field numbers must match the actual server wire f…
  - cause: Use a shared multiplexed `Channel` in tonic clients so that …
- Wiring verification test suites benefit from including smoke tests, reconnection tests, and backoff/retry behavior tests to validate infrastructure integration beyond unit test coverage. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - effect: feat: add integration tests for cloud-gateway (task group 6)…
- Use Option<T> as a backing store for session state to cleanly represent active/inactive states without requiring a separate boolean flag. _(spec: 08_parking_operator_adaptor, confidence: 0.60)_
  - cause: feat: implement session module for parking-operator-adaptor …
- Use helper functions like skipIfNATSUnavailable to centralize external service availability checks across multiple test files, reducing duplication and ensuring consistent behavior. _(spec: 03_locking_service, confidence: 0.60)_
  - cause: Skip integration tests gracefully using t.Skipf() when check…
- End-to-end smoke tests should verify the complete flow step-by-step (REST → NATS publish → response subscribe → status query) with clear logging at each stage to aid debugging. _(spec: 03_locking_service, confidence: 0.60)_
  - cause: Integration tests that capture subprocess output or rely on …
  - effect: When testing message headers (e.g., NATS Authorization heade…
- Separate smoke tests (end-to-end integration) from property-based tests when validating complex systems like message queues and command flows. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - cause: Running tests with the race detector (go test -race) helps c…
- StateManager requires separate methods for each operation (create_adapter, transition, force_error, get_adapter, list_adapters, remove_adapter, get_running_adapter, get_offload_candidates, subscribe) rather than a single generic handler. _(spec: 07_update_service, confidence: 0.60)_
  - cause: RealPodmanExecutor should use tokio::process::Command for ex…
  - effect: UpdateService install_adapter flow requires validation and e…
- A cloud gateway client typically requires three concurrent async loops: command processing, response handling, and telemetry collection. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - cause: A complete startup sequence for a cloud gateway client follo…
- Edge case test coverage for configuration errors (overlay syntax, missing fields) should verify actual process failures rather than just parsing/validation logic. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: feat: add E2E happy-path scenario test and configure service…
- Test suites for locking/synchronization services should cover happy paths, rejection scenarios, connection failures, and graceful shutdown to ensure robustness. _(spec: 03_locking_service, confidence: 0.60)_
  - cause: feat: add integration tests for locking-service and fix comp…
- VSS (Vehicle Signal Specification) signals can be subscribed as streaming responses alongside command responses in a gRPC client. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
- Apply default values to configuration durations (e.g., command timeout) after loading from config, checking for non-positive values to prevent invalid timing configurations. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - effect: Log startup information (version, port, NATS URL, token coun…
- Tests for graceful shutdown should handle unavailable external services (like NATS) by gracefully skipping affected test sections rather than failing entirely. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - cause: Graceful shutdown in Go servers should combine NATS drain() …
- Tests for startup logging should run before attempting external service connections, allowing validation of initialization logic without external dependencies. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - cause: Load application configuration from environment variables (e…
  - effect: Register auth middleware in HTTP route setup to ensure authe…
- VSS configuration can be loaded from multiple sources by comma-separating paths in the --vss flag (e.g., base config and overlay file). _(spec: 02_data_broker, confidence: 0.60)_
  - cause: feat: configure databroker dual listeners, version pin, and …
- Manual overrides (gRPC commands) should not persist across restart; after override, the next autonomous event should process normally. _(spec: 07_update_service, confidence: 0.60)_
  - cause: Lock/unlock events should be idempotent: consecutive lock ev…
  - effect: In-memory session state should be treated as authoritative; …
- Trim trailing slashes from base URLs (e.g., `base_url.trim_end_matches('/')`) before storing to ensure consistent path concatenation in HTTP client implementations. _(spec: 08_parking_operator_adaptor, confidence: 0.60)_
  - effect: feat: implement HTTP handlers and NATS client for cloud-gate…
- Container lifecycle tests requiring podman should verify both 'podman compose' and 'podman-compose' command availability as fallback options. _(spec: 02_data_broker, confidence: 0.60)_
- Permissive mode testing should verify that requests with invalid/arbitrary authentication tokens are accepted without PermissionDenied or Unauthenticated errors. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: Live integration tests should skip gracefully when external …
- Test subscription streams with grpcurl by using --max-time flag to auto-terminate after receiving updates, and trigger value changes from concurrent goroutines. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: When testing gRPC APIs, use grpcurl with -plaintext flag and…
- Test graceful shutdown behavior using subprocess tests to verify signal handling and clean termination in isolation. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - cause: Implement graceful shutdown by capturing SIGTERM and SIGINT …
- Distance to polygon edge can be computed by combining flat-Earth segment projection with Haversine distance for practical accuracy in localized zones. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - cause: Ray casting algorithm can be used to implement PointInPolygo…
  - cause: Haversine formula is the standard approach for calculating g…
- Extract optional command_id from raw JSON before full parsing to enable sending error responses even when JSON is malformed; use serde_json::Value and filter empty strings. _(spec: 01_project_setup, confidence: 0.60)_
  - effect: Distinguish between parse-time errors (non-JSON) and validat…
- Design error types with specific reasons (e.g., `invalid_command`, `vehicle_moving`) rather than generic messages, so they can be matched and formatted consistently in responses. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: Implement idempotency by checking state before making mutati…
  - effect: When a gRPC service returns field-level errors (separate fro…
- Extract command identifiers early in the request pipeline to enable tracing and idempotency checking throughout the command lifecycle. _(spec: 03_locking_service, confidence: 0.60)_
  - cause: tonic-build requires vendored proto files and a build.rs scr…
  - effect: Idempotent state updates in command processing prevent dupli…
- Property-based testing (verifying properties that must always hold) complements example-based tests; use them to ensure invariants like 'any token used on a mismatched VIN always returns 403' hold across many inputs. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - cause: Interface-based mocking (e.g., `NATSPublisher`) allows test …
  - cause: When testing timeout behavior, allow margin (e.g., sleep 2x …
- Stub implementations should return 'not implemented' errors during development; this surfaces unimplemented code paths during testing. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - effect: Always set Content-Type: application/json in REST JSON respo…
- Use time.Timer with sync.Mutex to implement timeout-based response storage; store a timeout response when the timer fires unless the real response arrived first. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - effect: Implement property-based tests (in addition to example tests…
- Comprehensive test coverage organized by spec sections (TS-06-3, TS-06-5, TS-06-8+, TS-06-E1+, TS-06-P1+) helps structure the TDD red-green-refactor cycle systematically. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - cause: Running `go vet` with no warnings on stub implementations is…
- Deterministic unit tests are suitable for validating core business-logic modules (config, command, safety, response) that operate independently, while property-based tests and process-level tests are more appropriate for downstream integration that depends on multiple modules. _(spec: 03_locking_service, confidence: 0.60)_
  - cause: When implementing a multi-task-group spec with stub function…
- Clippy warnings in skeleton/stub code (dead_code, unused_variables) should be resolved early during initial setup rather than left as technical debt. _(spec: 01_project_setup, confidence: 0.60)_
  - effect: A root Makefile should include both operational targets (bui…
- VSS (Vehicle Signal Specification) signals can be customized via overlay JSON files (vss-overlay.json) rather than modifying the base specification directly. _(spec: 01_project_setup, confidence: 0.60)_
- When setting up infrastructure with Docker Compose for NATS and Kuksa Databroker, create both the compose.yml file and service-specific configuration files (e.g., nats-server.conf) in separate subdirectories for maintainability. _(spec: 01_project_setup, confidence: 0.60)_
  - cause: A root Makefile should include both operational targets (bui…
  - effect: A root Makefile should include both operational targets (bui…
- Use `std::time::SystemTime::now()` and `UNIX_EPOCH` to generate timestamps as seconds since epoch for response payloads. _(spec: 03_locking_service, confidence: 0.60)_
  - effect: Use `#[serde(skip_serializing_if = "Option::is_none")]` on o…
- Use builder pattern (method chaining with `self`) for test doubles to construct complex mock configurations in a readable, fluent style. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - cause: Implement mock clients using `RefCell<Vec<T>>` to record met…
  - cause: In Rust trait implementations, suppress unused parameter war…
- Go skeleton mains should be simplified to only import necessary packages and print version strings; remove stub error messages and explicit os.Exit(0) calls. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - cause: Rust skeleton binaries should check for flag arguments (star…
  - cause: All Rust crate sources must contain a #[test] annotation wit…
- Test files should be organized in dedicated test directories (e.g., tests/setup/) and map to specific test case identifiers from specifications. _(spec: 01_project_setup, confidence: 0.60)_
  - cause: All binary modules should include placeholder tests (#[test]…
- Cargo workspaces with multiple binaries (stub or otherwise) should have shared library code in a lib.rs to avoid duplication and maintain consistency across CLI tools. _(spec: 09_mock_apps, confidence: 0.60)_
  - effect: When setting up multi-language test suites (Rust + Go), orga…
  - effect: Use subprocess-based integration tests for CLI applications …
- When setting up multi-language test suites (Rust + Go), organize integration tests in separate modules per language/component to avoid coupling and maintain clear test boundaries. _(spec: 09_mock_apps, confidence: 0.60)_
  - cause: Cargo workspaces with multiple binaries (stub or otherwise) …
  - effect: Writing failing tests before implementation (TDD red phase) …

## Decisions

- Proto field numbers must match the actual server wire format to interoperate with the real binary; document field number mappings when defining minimal proto subsets. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: Eclipse Kuksa Databroker 0.5.0 exposes only `kuksa.val.v2.VA…
- When organizing large task groups with multiple failing tests, distinguish between failures from current scope vs. stub dependencies belonging to future task groups to avoid premature debugging. _(spec: 07_update_service, confidence: 0.90)_
  - cause: UpdateService install_adapter flow requires validation and e…
- Safety checks (vehicle moving, door open) are required only for lock commands, not unlock commands; this asymmetry reflects the safety-critical nature of preventing unlocking. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Implement idempotent lock/unlock by checking current state f…
- Distinguish between parse-time errors (non-JSON) and validation errors in command handling; non-JSON payloads should be silently discarded without publishing a response. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Extract optional command_id from raw JSON before full parsin…
  - effect: Check for field-level errors in gRPC responses (response.err…
- Continue processing after response publish failures; log the error and return the response so the service remains available. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Implement idempotent lock/unlock operations: only call `set_…
  - effect: Use `serde(skip_serializing_if = "Option::is_none")` to omit…
- Treat missing sensor signals as safe defaults: absent speed → 0.0, absent door state → closed (false). _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Safety checks should prioritize speed over other constraints…
  - effect: Implement idempotent lock/unlock operations: only call `set_…
- Safety checks should prioritize speed over other constraints; if speed ≥ 1.0 km/h, return VehicleMoving before checking door state. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Speed boundary is exactly 1.0 km/h: speeds < 1.0 are safe, s…
  - effect: Treat missing sensor signals as safe defaults: absent speed …
- Go skeleton main.go files should print version strings instead of writing stub messages to stderr. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Skeleton binaries should print version strings and reject un…
- Code consolidation should merge files that are always modified together (high co-change coupling) or files that exist only to be imported by one consumer, but avoid creating god-files exceeding ~300 lines. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: When simplifying code, prioritize maintainability first, the…
  - effect: Over-abstractions (interfaces with one implementation, singl…
- When simplifying code, maintainability always wins over reduced line count; a change is good if you would be comfortable handing it to a new team member on their first day. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Code simplification should eliminate over-engineering such a…
- Vendoring proto files (kuksa.val.v1) directly into the project allows offline proto compilation without external dependencies. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - cause: Tonic requires build.rs with tonic-build to compile vendored…
  - effect: Tonic requires build.rs with tonic-build to compile vendored…
- A no-op build.rs script can be included in Cargo.toml projects even when no build-time logic is needed, serving as a placeholder for future build customization. _(spec: 08_parking_operator_adaptor, confidence: 0.60)_
  - cause: Using clippy with -D warnings flag during initial project se…
- Static YAML validation tests (e.g., compose.yml checks) should use string.Contains() over full YAML parsing to avoid tight coupling to compose syntax details. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: Use go.work file with 'use' directives to manage multiple Go…
- Scaffolding task groups in advance (e.g., creating model package structure early) reduces implementation friction in subsequent task groups. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - effect: Map-based indexing is an effective pattern for store impleme…
- RegistrationMessage should include current Unix timestamp and 'online' status on creation to represent the initial state accurately. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - cause: TelemetryState should omit never-set fields from JSON output…
- When implementing a cloud gateway client spec in Rust, use Vec<serde_json::Value> for polymorphic fields (like CommandPayload.doors) to avoid implicit type validation and maintain flexibility during deserialization. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
- In a polyglot monorepo, deferring infrastructure and build system tests (Makefile, deployment configuration) until later task groups allows core workspace structure validation to pass independently. _(spec: 01_project_setup, confidence: 0.60)_
  - cause: A Rust Cargo workspace can be organized with multiple member…
  - cause: Go modules in a monorepo can be coordinated using a go.work …

## Conventions

- Graceful shutdown handling for both SIGTERM and SIGINT signals should be explicitly verified in integration tests. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: Comprehensive test coverage should include unit tests, prope…
- Requirements traceability should be verified by mapping each requirement identifier to passing tests to ensure complete coverage. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: Comprehensive test coverage should include unit tests, prope…
- Proto codegen must be configured in build.rs to compile .proto files (both project-specific and external dependencies like kuksa.val.v1) before the main crate compilation. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: Channel-based command dispatch is an effective pattern for r…
  - effect: Tonic gRPC clients require explicit connection retry logic a…
- Use t.Cleanup() to register teardown operations (process kill, file deletion) that run after the test, ensuring cleanup happens even if the test fails. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Use a goroutine-safe wrapper (safeBuffer with sync.Mutex) ar…
  - cause: Implement port readiness polling with net.DialTimeout using …
  - effect: Skip tests on infrastructure issues (network problems, unava…
- Use a separate Unix-specific build file (helpers_unix_test.go with //go:build !windows) to conditionally define platform-dependent test helpers like signal sending. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Implement skip conditions as helper functions that call t.Sk…
- Go integration test modules should be added to go.work to ensure they are included in the workspace build and test runs. _(spec: 07_update_service, confidence: 0.90)_
  - cause: feat: add E2E test infrastructure with Containerfiles and co…
- Use crypto/rand for UUID generation in Go rather than other random sources to ensure cryptographic quality. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: test: write spec tests for mock apps (task group 1)

- Add V…
- Structured startup logging with config details and server binding information aids debugging and operational visibility in production services. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Signal handling for server shutdown should trap both SIGTERM…
- Signal handling for server shutdown should trap both SIGTERM and SIGINT to ensure clean termination from both system requests and user interrupts. _(spec: 07_update_service, confidence: 0.90)_
  - cause: Graceful shutdown in Rust async servers should include a dra…
  - effect: Structured startup logging with config details and server bi…
- Passing all spec tests with zero clippy warnings indicates proper adherence to Rust idioms and best practices in the implementation. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: Proto files can be vendored directly into the project reposi…
  - cause: Tonic can be used to implement real gRPC client helpers for …
- Task group 6 (Wiring Verification) for spec 02_data_broker requires verifying compose.yml contains pinned image versions, dual listeners, correct port mappings, UDS volume mounts, VSS overlay flags, and absence of auth flags. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: refactor: decouple E2E container builds from compose

Build …
- When adding integration tests to an existing Rust project, verify that all existing unit tests continue to pass and clippy reports no warnings to ensure no regressions were introduced. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Integration tests in Rust can be marked with #[ignore] attri…
- TCP port availability checks in integration tests should use a reasonable timeout (e.g., 10 seconds) to account for container startup time. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: feat: add E2E test infrastructure with Containerfiles and co…
- Running tests with the race detector (go test -race) helps catch concurrency issues and should be part of the standard test suite validation. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: fix: eliminate flaky TestStartupLogging in parking-fee-servi…
  - effect: Separate smoke tests (end-to-end integration) from property-…
- Requirements traceability should map all requirement identifiers (e.g., 03-REQ-*.*) to passing tests and document gaps (e.g., TS-03-E2) explicitly in errata rather than leaving them unmarked. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Race conditions in config tests can occur when using environ…
  - cause: Makefile maintenance should track implementation status by i…
- Implementing Display traits on custom error types (NatsError, BrokerError) improves error ergonomics and debugging in async systems. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Tracing-subscriber initialization should be configured durin…
- Integration tests involving external tools like Podman should implement conditional skip logic based on tool availability to prevent CI failures in environments where the tool isn't installed. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: refactor: decouple E2E container builds from compose

Build …
- Go workspace modules should be added to go.work when creating new test packages to maintain proper dependency resolution. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: feat: add integration tests for parking-operator-adaptor (ta…
- Proto compatibility gaps between custom service implementations and real API versions should be documented in an errata file for transparency and future reference. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: feat: validate DATA_BROKER integration tests against live da…
- Tonic requires build.rs with tonic-build to compile vendored .proto files, and prost for serialization support. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Vendoring proto files (kuksa.val.v1) directly into the proje…
  - effect: gRPC clients with tonic can implement exponential backoff fo…
  - effect: Vendoring proto files (kuksa.val.v1) directly into the proje…
- Custom VSS signals must explicitly declare correct types (boolean, string, etc.) in the overlay definition to pass validation. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: kuksa-databroker requires intermediate branch nodes to be ex…
- In Go 1.22+, use the simplified http.ServeMux pattern syntax (e.g., 'GET /path', 'POST /path') instead of registering handlers manually for each method. _(spec: 05_parking_fee_service, confidence: 0.90)_
- Load application configuration from environment variables (e.g., CONFIG_PATH) at startup rather than hardcoding paths, enabling flexible deployment across environments. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Tests for startup logging should run before attempting exter…
- Proptest property-based tests should be marked `#[ignore]` and documented with instructions to run explicitly to avoid cluttering regular test runs. _(spec: 07_update_service, confidence: 0.90)_
- Use `#[allow(async_fn_in_trait)]` when defining async methods in traits to avoid compiler warnings about unstable feature usage. _(spec: 07_update_service, confidence: 0.90)_
  - effect: Mock objects in tests should not require `Send + Sync` bound…
- Mark property-based tests with `#[ignore]` and document how to run them explicitly (e.g., `cargo test -- --ignored`) to avoid excessive runtime in standard test runs. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: test: write failing spec tests for parking-operator-adaptor …
- Use `std::env::remove_var()` in config tests to clean up environment before and after assertions to prevent test pollution across the test suite. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: test: write failing spec tests for parking-operator-adaptor …
- Using clippy with -D warnings flag during initial project setup ensures code quality standards are met from the start and prevents accumulation of warnings in the codebase. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - cause: When setting up a test-driven development (TDD) project stru…
  - effect: A no-op build.rs script can be included in Cargo.toml projec…
- Property tests should be marked with #[ignore] during initial TDD phases when implementing against a spec, allowing them to be skipped until the core implementation is ready. _(spec: 07_update_service, confidence: 0.90)_
  - cause: When implementing a failing spec test suite, organize source…
  - effect: When implementing a failing spec test suite, organize source…
- Race condition testing with Go's race detector should be part of the standard test suite for concurrent code to catch synchronization issues early. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Graceful shutdown of message broker clients should use a Dra…
- All HTTP responses should explicitly set Content-Type: application/json header when returning JSON payloads to ensure proper client interpretation. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Handler implementations should validate incoming payloads be…
- Use go.work file with 'use' directives to manage multiple Go modules in a monorepo, and maintain go.work.sum for dependency tracking. _(spec: 02_data_broker, confidence: 0.90)_
  - effect: Static YAML validation tests (e.g., compose.yml checks) shou…
- HTTP handlers should consistently set Content-Type: application/json in response headers when returning JSON payloads. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Go 1.22 ServeMux supports direct route registration without …
- Document spec divergences between implementation and specification (e.g., image tags, CLI flags, API method names) in an errata file to maintain clarity on deviations. _(spec: 02_data_broker, confidence: 0.90)_
  - cause: test: write failing spec tests for data-broker (task group 1…
  - cause: feat: validate DATA_BROKER integration tests against live da…
- NATS subject paths are VIN-scoped and follow a consistent naming convention: vehicles.{VIN}.{endpoint} for commands, status, command_responses, and telemetry. _(spec: 08_parking_operator_adaptor, confidence: 0.90)_
  - effect: The NatsClient struct holds both an async_nats::Client and t…
- Response relay must maintain fidelity: JSON strings are published verbatim without modification or transformation. _(spec: 07_update_service, confidence: 0.90)_
  - cause: NATS subjects are VIN-scoped with consistent naming pattern:…
- NATS subjects are VIN-scoped with consistent naming pattern: vehicles.{VIN}.{purpose} (commands, status, command_responses, telemetry). _(spec: 07_update_service, confidence: 0.90)_
  - effect: Response relay must maintain fidelity: JSON strings are publ…
- Go's vet tool should pass clean with no warnings when implementing geometric algorithms with proper type handling. _(spec: 05_parking_fee_service, confidence: 0.90)_
- Run tests with the -race flag to detect and verify fixes for race conditions in concurrent code. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Timeout cancellation race conditions can occur when a timer …
- Remove #![allow(dead_code)] once modules are fully wired to enable clippy to catch genuinely unused code; this was a temporary measure during skeleton phase. _(spec: 01_project_setup, confidence: 0.90)_
- Organize NATS client functionality into distinct methods for subscribe_commands(), publish_registration(), publish_response(), and publish_telemetry() to maintain clear separation of concerns. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Use tracing instrumentation at INFO/WARN/ERROR levels across…
- Auth middleware should return JSON-formatted 401 (Unauthorized) and 403 (Forbidden) error responses rather than plain text or HTML. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: Auth middleware should extract Bearer tokens from request he…
- Document Makefile test scope exclusions and changes in errata files to track why certain packages are excluded from test targets. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Stub tests added in sub-packages by downstream task groups c…
- Use unexported types (lowercase) for context keys in Go (e.g., `type contextKey string`) to prevent external packages from accidentally colliding with your context key names. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: Middleware in Go should follow the `func(http.Handler) http.…
- Error responses should follow a consistent format (e.g., {"error":"message"}) across all endpoints. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Always set Content-Type: application/json in REST JSON respo…
- Always set Content-Type: application/json in REST JSON responses; test this header is present across all endpoints. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Bearer token validation should extract the token from the Au…
  - cause: Stub implementations should return 'not implemented' errors …
  - effect: Error responses should follow a consistent format (e.g., {"e…
- When testing NATS integration with exponential backoff retry logic, document the minimum expected elapsed time (sum of backoff intervals). _(spec: 05_parking_fee_service, confidence: 0.90)_
- Test helpers that modify test state (like makeTestConfig) should return copies or be clearly documented to avoid shared state between test cases. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Implement property-based tests (in addition to example tests…
- Use unexported types (lowercase) for context keys to prevent external packages from accidentally using the same key string, avoiding context key collisions. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - effect: Bearer token validation should extract the token from the Au…
- Running `go vet` with no warnings on stub implementations is a good checkpoint before proceeding to write failing tests, ensuring code quality standards are met early. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: When implementing Go packages with stub files for TDD, ensur…
  - effect: Comprehensive test coverage organized by spec sections (TS-0…
- Ensure go vet passes with no warnings during test scaffolding phase to catch static analysis issues early before implementing actual functionality. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Use panic('not implemented') as a stub implementation patter…
  - cause: Organize Go service packages with modular structure (model, …
- A root Makefile should include both operational targets (build, test, clean, infra-up, infra-down) and code quality targets (check, proto), with sub-targets for modularity and clarity. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: When setting up infrastructure with Docker Compose for NATS …
  - cause: Clippy warnings in skeleton/stub code (dead_code, unused_var…
  - effect: When setting up infrastructure with Docker Compose for NATS …
- Prefix unused function parameters with underscore (e.g., _param) to satisfy clippy's -D warnings lint instead of removing them. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Use ENV_MUTEX in config tests to prevent race conditions whe…
  - effect: TelemetryState should omit never-set fields from JSON output…
- Suppress `#![allow(dead_code)]` at crate level during TDD skeleton phase when functions are stubbed with `todo!()`, and remove it once all modules are wired and implementations complete. _(spec: 03_locking_service, confidence: 0.90)_
  - effect: Use `#[allow(async_fn_in_trait)]` when defining async method…
- Mark property-based tests with `#[ignore]` so they don't run by default with `cargo test`; users run them explicitly with `cargo test -- --ignored`. _(spec: 03_locking_service, confidence: 0.90)_
- Property-based tests with proptest should be marked `#[ignore]` so they don't run by default with `cargo test`, and can be executed explicitly with `cargo test -- --ignored`. _(spec: 05_parking_fee_service, confidence: 0.90)_
  - cause: Use `RefCell` for interior mutability in test doubles when t…
- Use `#[serde(skip_serializing_if = "Option::is_none")]` on optional response fields to exclude them from JSON serialization when absent, maintaining clean API responses. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - effect: For safety-critical code, document signal paths and safety c…
- For property-based tests using proptest, mark them with `#[ignore]` to prevent them from running with `cargo test` by default, requiring explicit invocation with `cargo test -- --ignored`. _(spec: 06_cloud_gateway, confidence: 0.90)_
  - cause: In Rust tests that manipulate environment variables, use `st…
- Mark property-based tests with `#[ignore]` so they do not run with `cargo test` by default; users must explicitly run them with `cargo test -- --ignored`. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: Record mock calls using `RefCell<Vec<T>>` for interior mutab…
- Ensure all tests compile and pass clippy with -D warnings before moving to GREEN phase, even when tests are expected to fail functionally. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Use ignored property tests (via #[ignore]) to preserve test …
- Use ignored property tests (via #[ignore]) to preserve test cases during RED phase while focusing on deterministic unit tests, deferring property test execution to later phases. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: Use MockBrokerClient and todo!() stubs to scaffold failing t…
  - effect: Ensure all tests compile and pass clippy with -D warnings be…
- Integration tests that are not yet ready should be marked with #[ignore] to allow the test suite to run without blocking on unimplemented functionality. _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - cause: A test-driven development approach for Rust modules involves…
- All Go modules must contain at least one test file (*_test.go) with a func Test... declaration; minimal tests that pass are acceptable for placeholder compliance. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Pre-existing RED-phase (failing) tests from future specs sho…
- All Rust crates must contain at least one unit test marked with #[test] annotation; a minimal test that asserts true is acceptable for placeholder compliance. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: Pre-existing RED-phase (failing) tests from future specs sho…
- Proto3 files should include package declarations and go_package options for proper code generation and namespace management across multiple service definitions. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: Proto files should be validated with protoc during developme…
- Each Rust crate must contain at least one #[test] annotation in its source files; each Go module must contain at least one func Test function in *_test.go files. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: test: write failing spec tests for parking-operator-adaptor …
- Skeleton binaries must print a version string containing their component name (e.g., 'cloud-gateway-client v0.1.0') to stdout when invoked (01-REQ-4.1, 01-REQ-4.2, 01-REQ-4.4). _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Spec 01 skeleton binaries print version and exit 0 with no a…
- All Go modules must have at least one _test.go file with a func Test function to satisfy compilation requirements (01-REQ-8.2). _(spec: 04_cloud_gateway_client, confidence: 0.90)_
- All Rust crate sources must contain a #[test] annotation with at least one unit test to satisfy compilation requirements (01-REQ-8.1). _(spec: 04_cloud_gateway_client, confidence: 0.90)_
  - effect: Go skeleton mains should be simplified to only import necess…
- All binary modules should include placeholder tests (#[test] in Rust, TestCompiles in Go) even before implementation. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Skeleton binaries should print version strings and reject un…
  - effect: Test files should be organized in dedicated test directories…
- Skeleton binaries should print version strings and reject unknown flags with usage output to stderr, exiting with code 1. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: All binary modules should include placeholder tests (#[test]…
  - effect: Go skeleton main.go files should print version strings inste…
- Writing failing tests before implementation (TDD red phase) across multiple languages/crates requires ensuring all code compiles cleanly and linters pass before verification, preventing false negatives. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: When setting up multi-language test suites (Rust + Go), orga…
- Placeholder directories in a monorepo structure can be tracked with .gitkeep files to ensure directory presence in version control without requiring actual content. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: In a polyglot monorepo, deferring infrastructure and build s…
- Document test specifications (TS-01-*) and requirement mappings (01-REQ-*) in test comments to maintain traceability between tests and project requirements. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Use subtests (t.Run) to organize related assertions and prov…
  - effect: Structure setup validation tests to verify both configuratio…
- In Go test files, use t.Helper() in helper functions to ensure error messages report the caller's location rather than the helper function's location. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Use runtime.Caller(0) with filepath navigation to dynamicall…
  - effect: Use subtests (t.Run) to organize related assertions and prov…
- Use conventional commit messages with format `{type}({scope}): {description}` (e.g., `fix(auth): validate token before use`) and include `(fixes #{issue_number})` to auto-close issues when PR merges. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: Autonomous code fixes should post a transparent analysis com…
- Never refactor test code primarily for DRYness—tests should be verbose, explicit, and independently readable; duplicated setup in tests is a feature, not a bug. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: When simplifying code, prioritize maintainability first, the…
- ADRs should be immutable once accepted; document rejected options with equal care as the chosen one so future engineers can avoid re-evaluating the same alternatives. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: ADRs must document at least: context (the problem), decision…
- ADRs must document at least: context (the problem), decision drivers, options considered with pros/cons, the chosen decision with rationale, and consequences (positive, negative, and follow-up actions). _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: Architecture Decision Records (ADRs) should be stored in `do…
  - effect: When documenting architectural decisions, code is the single…
  - effect: ADRs should be immutable once accepted; document rejected op…
- Architecture Decision Records (ADRs) should be stored in `docs/adr/`, `doc/adr/`, `adr/`, `docs/decisions/`, or `decisions/` directories in priority order, with files named `NN-imperative-verb-phrase.md` using zero-padded numbers. _(spec: 09_mock_apps, confidence: 0.90)_
  - effect: ADRs must document at least: context (the problem), decision…
- Bash permission settings in `.claude/settings.local.json` must explicitly whitelist commands like `git`, `python`, `uv`, `make`, `ruff`, and `gh` to enable different tools; use `Bash(command:*)` format for granular control. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: Autonomous code fixes require pre-flight checks: verify work…
  - effect: The `af-fix` autonomous fixer uses a single-pass mandate: co…
- ADR status field must be set to one of: Proposed, Accepted, Deprecated, or Superseded by [ADR-NNNN]; immutability by default means accepted ADRs should not be edited in substance, instead creating new ADRs and marking old ones superseded. _(spec: 01_project_setup, confidence: 0.90)_
- ADRs should document at least two real alternatives with honest trade-offs, connect decision rationale directly to stated decision drivers, and include both positive and negative consequences to be trustworthy. _(spec: 01_project_setup, confidence: 0.90)_
  - cause: When creating ADRs, the code is the single source of truth; …
- ADR filenames follow the pattern `NN-imperative-verb-phrase.md` where NN is a zero-padded sequential number; the verb should be present-tense imperative (use, adopt, replace, migrate, define, choose). _(spec: 01_project_setup, confidence: 0.90)_
- Architecture Decision Records (ADRs) should be stored in `docs/adr/` directory by default, with fallback locations of `doc/adr/`, `adr/`, `docs/decisions/`, or `decisions/` if the primary does not exist. _(spec: 01_project_setup, confidence: 0.90)_
- REST APIs with bearer-token authentication require proper header validation in both client requests and server handlers to ensure security policies are enforced. _(spec: 09_mock_apps, confidence: 0.60)_
  - effect: feat: implement parking-app-cli with 9 subcommands (task gro…
- Errata documents should map affected spec requirements and include root-cause analysis, mitigation steps, and a test impact table to trace fixes back to original failures. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: VSS overlay JSON files for kuksa-databroker must use properl…
  - cause: Use `VAL/PublishValue` instead of `VAL/Actuate` when writing…
- Mark integration tests that require external containers (NATS, DATA_BROKER) with `#[ignore]` and include inline availability checks to gracefully skip rather than panic when infrastructure is unavailable. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: Integration tests that spawn service subprocesses should cap…
  - cause: Exponential backoff retry tests with real-time waits (e.g., …
  - effect: Errata documents should map affected spec requirements and i…
- REST client implementations for external services should use POST endpoints for session lifecycle operations (start/stop) with proper error handling and retry mechanisms. _(spec: 08_parking_operator_adaptor, confidence: 0.60)_
  - cause: Implement retry logic with exponential backoff using inline …
- Test naming conventions should use descriptive IDs (e.g., TS-02-SMOKE-1) and clear test function names that indicate what aspect is being tested (HealthCheck, FullSignalInventory). _(spec: 02_data_broker, confidence: 0.60)_
  - cause: feat: add integration tests for parking-operator-adaptor (ta…
- Tracing-subscriber initialization should be configured during application startup before spawning main processing loops. _(spec: 04_cloud_gateway_client, confidence: 0.60)_
  - effect: A complete startup sequence for a cloud gateway client follo…
  - effect: Implementing Display traits on custom error types (NatsError…
- Load configuration from environment variables via CONFIG_PATH at application startup to support flexible deployment configurations. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - effect: Use slog for structured startup logging in Go services to pr…
- Tasks may already exist from previous task groups; verify and reuse existing work before creating duplicates. _(spec: 01_project_setup, confidence: 0.60)_
  - effect: Setup verification tests should invoke toolchains as subproc…
- Document test scope limitations and known constraints in an errata or known-issues file to set clear expectations for the team. _(spec: 01_project_setup, confidence: 0.60)_
  - effect: A root Makefile should include both operational targets (bui…
- Suppress the `dead_code` lint during test-driven development when modules are still skeleton implementations with `#[allow(dead_code)]` at the crate level. _(spec: 05_parking_fee_service, confidence: 0.60)_
  - cause: Use `#[allow(async_fn_in_trait)]` when defining async trait …
- In Rust trait implementations, suppress unused parameter warnings with `_param` prefix rather than `#[allow(unused)]` to maintain clarity about which parameters are intentionally unused. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - effect: Use builder pattern (method chaining with `self`) for test d…
- In Cargo.toml, explicitly define `[[bin]]` section with `name` and `path` to ensure correct binary naming and location when the crate contains multiple binaries. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - effect: For safety-critical code, document signal paths and safety c…
- Skeleton comments should reference 'a future spec' rather than specific task group numbers to remain valid across spec iterations. _(spec: 09_mock_apps, confidence: 0.60)_
  - cause: Skeleton binaries should print a version string containing t…
- When updating stub comments, reference 'a future spec' rather than specific task groups to avoid hardcoding incomplete implementation schedules. _(spec: 03_locking_service, confidence: 0.60)_
  - cause: feat: add Go workspace and skeleton implementations (task gr…

## Anti-Patterns

- Over-abstractions (interfaces with one implementation, single-use wrappers, factory functions producing one type) should be inlined and deleted rather than kept for hypothetical future flexibility. _(spec: 09_mock_apps, confidence: 0.90)_
  - cause: Code consolidation should merge files that are always modifi…
- Never refactor test code for DRYness; tests should be verbose, explicit, and independently readable, where duplicated setup is a feature not a bug. _(spec: 01_project_setup, confidence: 0.90)_
- Rust skeleton functions should explicitly handle unknown or unrecognized flags rather than using todo! macros to prevent test failures when edge cases are exercised. _(spec: 03_locking_service, confidence: 0.60)_

## Fragile Areas

- Integration tests that capture subprocess output or rely on timing need generous buffers (2s vs 500ms) to account for CPU starvation under parallel test execution. _(spec: 03_locking_service, confidence: 0.90)_
  - cause: When subscribing to NATS messages in tests, call Flush() aft…
  - effect: End-to-end smoke tests should verify the complete flow step-…
- Stub tests added in sub-packages by downstream task groups can break parent test targets if they use recursive wildcards; coordinate test scope changes across task groups. _(spec: 01_project_setup, confidence: 0.90)_
  - effect: When Go test wildcards ('...') cause failures in stub sub-pa…
  - effect: Document Makefile test scope exclusions and changes in errat…
- Flag parsing for subcommands like 'serve <flags>' requires careful handling to ensure flags are correctly associated with the subcommand rather than the parent command. _(spec: 09_mock_apps, confidence: 0.60)_
  - effect: fix: pass serve subcommand in integration test buildCmd help…
- Exponential backoff retry tests with real-time waits (e.g., 1s, 2s, 4s, 8s) require generous timeout tolerances (e.g., 8–30s for 15s expected) to account for system scheduling variance. _(spec: 02_data_broker, confidence: 0.60)_
  - effect: Mark integration tests that require external containers (NAT…
- Pre-existing baseline failures in unrelated modules (e.g., cloud-gateway-client) may need to be restored/re-introduced as part of completing current task groups if they were intentionally removed. _(spec: 02_data_broker, confidence: 0.60)_
- Exponential backoff retry logic can be ambiguous when specified with only partial examples (e.g., 'attempts with 1s, 2s, 4s backoff'); document the full sequence including caps and total attempts. _(spec: 06_cloud_gateway, confidence: 0.60)_
  - cause: feat: implement operator client, broker, and gRPC handlers (…
- Host-side directory setup (mkdir -p /tmp/kuksa) may need to be added to Makefile infra-up target to ensure socket accessibility before container startup. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: refactor: decouple E2E container builds from compose

Build …
- Container-dependent infrastructure (like UDS sockets and volumes) should be identified early in test development and documented as not-yet-configured rather than failing silently. _(spec: 02_data_broker, confidence: 0.60)_
  - cause: feat: validate DATA_BROKER integration tests against live da…
  - cause: feat: add E2E test infrastructure with Containerfiles and co…
- Token-to-VIN lookups via linear scan are acceptable for small token mappings but may become a fragile area as scale increases; consider indexing if performance becomes critical. _(spec: 06_cloud_gateway, confidence: 0.60)_
