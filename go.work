go 1.25.0

use (
	.
	./proto
	./tests/cloud-gateway
	./tests/cloud-gateway-client
	./tests/databroker
	./tests/locking-service
	./tests/mock-apps
	./tests/setup
	./tests/update-service
)

// Go modules accessible via root module (.):
// ./backend - Backend services (parking-fee-service, cloud-gateway)
// ./mock - Mock CLI applications (parking-app-cli, companion-app-cli, parking-operator)
