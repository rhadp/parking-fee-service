[package]
name = "update-service"
version = "0.1.0"
edition = "2021"
description = "RHIVOS update service — manages adapter lifecycle (install, remove, status)"
publish = false

[[bin]]
name = "update-service"
path = "src/main.rs"

[dependencies]
parking-proto = { path = "../parking-proto" }
tonic = { workspace = true }
prost = { workspace = true }
tokio = { workspace = true }
clap = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
tokio-stream = { workspace = true }
